# AIのミス記録

ユーザーからの明示的な訂正のうち、繰り返し起こりうるものだけを記録する。

> **注意：** このファイルは詳細版。セッション開始時に自動読み込みされるのは要約の `knowledge/context.md`。
> ここに追記したら、`context.md` の「過去のミスから来た行動ルール」にも1行で反映すること。

形式：
YYYY-MM-DD: [一言で何を間違えたか]
NG: 実際にやってしまったこと
OK: 次回からの正しい対応
場面: このルールが適用される状況

---

2026-09-07: 出力物の検証を、作る側と同じ解き方でしかやっていなかった
NG: 「線を消す」謎で、STEPは「この4本」と名指しして解くのでテストは通るのに、問題用紙の指示は「あかいせんをけせ」と色で言うので、紙の上の人は同じ色の線を8本ぜんぶ消してしまい途中の段が読めなくなっていた（6回作って6回とも）
OK: 出力物を人が別の手がかり（色・見た目・言葉）で扱うなら、**テストもその手がかりで解き直して**突き合わせる。内部のid/keyだけで検証すると「機械は正しいが人には解けない」ものが素通りする
場面: 作ったものを人が紙や画面で扱う道具を検証するとき全般

2026-09-07: リトライを回す仕組みの中の広い try/catch が、バグを「作れません」に化けさせた
NG: 変数名の付けかえ漏れで TypeError が出ていたのに、build() の catch が握りつぶして「この組み合わせでは作れませんでした」としか出さず、仕様の限界だと思いこみかけた
OK: 捕まえた例外は必ず回数とメッセージを残し、それがいちばん多ければ「道具のほうの不具合です」と正直に出す。何度も試す仕組みほど、失敗の中身を捨てない
場面: 総当たり・リトライ・自動生成など「失敗しても次を試す」処理を書くとき

2026-09-07: 「2つの画面を1つにまとめて」を、上下に並べるだけで済ませた
NG: かんたん作成の下に、これまでの編集画面をそのままぶら下げた。結果はPNGの絵で見せ、直すには下にある別の盤面までスクロールする形のままだったので、「見ているもの」と「直すもの」が別物で、統合になっていなかった
OK: 「1つにまとめて」と言われたら、まず**同じ役目のものを2つ持っていないか**を探して、そこを1つにする。今回は「見せるための絵」と「編集するための盤面」を2つ持っていたのが本体で、絵をやめて盤面ひとつにすれば解決した。並べ替えではなく、重複を消す
場面: 「AとBを統合して」「1ページにまとめて」と言われたとき全般

2026-09-07: 書いたCSSが実際に効いているか確かめずに完了とした
NG: `.num { width: 74px }` と書いたのに、先に書いてある `input[type=number] { width: 100% }`（型＋属性セレクタ）のほうが優先度で強く、入力欄が横いっぱいに伸びてとなりのボタンと重なっていた
OK: 見た目に関わる指定は、書いたあと**計算後の値を実測して**確かめる（ブラウザで `getBoundingClientRect()` の幅を取る、要素どうしのすきまがマイナスでないか見る）。「書いた」は「効いた」ではない
場面: CSSの幅・高さ・表示/非表示を指定したとき（とくに既存のスタイルがある場所に足すとき）

2026-09-06: 答えの文字どうしの盤面上の距離を、道の「通った順」でしか考えなかった
NG: 文字を置く位置を、道を歩く順番で均等に割りふるだけにしていたので、道が折り返している場所では答えの文字どうしが紙の上ではすぐ隣にきて、固まって見えた
OK: 道の上の位置だけでなく、盤面のマス目としての距離（縦横斜めのマス数）も測って、離れたマスを選ぶ。「1回で欲張って決める」貪欲法は前半が予算を使いすぎて後半が破綻することがあるので、「出せる最大の均等距離を大きいほうから探す」方式にする
場面: 一本道・折り返す経路の上に複数の目印（文字・記号）を配置するとき

2026-09-06: 段が増えるたびに、条件を問わず自動で次の色に切りかえた
NG: 多段階の謎で、STARTやGOALが変わるだけの段でも「段が増えた」という理由だけで読む色を自動的に進めてしまい、ユーザーが「同じ色のままにしたい」と思っても選べなかった
OK: 「自動で切りかえる」ルールを作るときは、ユーザーが明示的に指示した条件と、構造上どうしても切りかえるしかない条件を分けて考える。前者以外は「変えない」を既定にする
場面: 複数の段階・状態を持つ生成物で、見た目（色・書式など）を自動で進行させる機能を作るとき

2026-09-06: まぎれ文字を「答えの空きマスは100%埋める・まわりはこさ設定ぶんだけ埋める」で作った
NG: ルートの上の空きマスは常に全部埋め、ルート外はこさ設定（例：28%）だけ埋めていたので、答えのルートだけ文字がびっしり詰まって見え、そこだけ目立ってしまった
OK: 「まぎれ・カモフラージュをまく密度」は、置く場所によらず盤面全体で同じ設定を使う。範囲ごとに別の埋め方をしたら、密度の差そのものが手がかりになってしまわないか確認する
場面: 迷路・クイズ・パズルで、正解の場所とそれ以外にまぎれ物・ダミーをまくとき

2026-09-06: まぎれ文字の材料を「答えで使われている字」から選んだ
NG: 答えの字（4〜8種類程度）だけを材料にまぎれ文字を選んでいたので、種類が少なく、まぎれ文字が答えとそっくりな字ばかりになった
OK: まぎれ・ダミーの材料は、対象そのものではなく広い一般的な集合（ひらがな46字など）を土台にする。対象由来の字を混ぜるとしても、種類がゼロにならない程度の補い役にとどめる
場面: クイズ・パズル・選択肢の自動生成で、正解と紛らわしいダミーを作るとき（前回2026-09-06の「読ませない色」の教訓と同じ根っこ＝ダミーの材料選びは正解の集合をそのまま使わない）

2026-09-06: まぎれ用のダミーを、答えを読む条件と同じ色で作った
NG: 「黒い文字だけ読む」設定の行に対して、まぎれ文字も黒でまいた（しかも答えに使った字を材料にした）ので、答えの間に関係ない字が割りこみ「おかだせんせい」が「おかせんだめんいせ」になった
OK: 「読ませるもの」と「読ませないもの」は必ず対にして1か所で決める。ダミー・ノイズ・おとりを作るときは、正解の判定条件を先に見て、それに引っかからない値だけを選ぶ。作ったあとは必ず自分で読み返して確かめる
場面: クイズ・謎解き・選択肢の自動生成で、ダミーや選択肢を混ぜるとき

2026-09-06: 同じ設定を2つの画面にそれぞれ持たせて、どちらが効くのか分からなくした
NG: 「かんたん作成」と「編集画面」に同じ意味の設定（文字の量・大きさ・わき道）を別々に置き、読み取りの試し欄と答えの指定欄のように機能が重なるパネルも並べたまま増やし続けた
OK: 同じ意味の値は1つだけ持ち、画面の入力欄はそれを映すだけにする（bindでつなぐ）。機能が重なるパネルは片方に統合する。両方に置くのは「1つの値・1つのハンドラを共有している」ときだけ
場面: 入口が2つある道具（かんたん／くわしく）を作るとき／画面に機能を足すとき

2026-06-10: 訓読文で助動詞の読みをカタカナ送り仮名として表記した
NG: 「不」の読み「ず」を送り仮名と同じ扱いでカタカナ「ズ」にして右下に表示した
OK: 助動詞・助詞として読む字（不＝ず、之＝の、也＝なり等）は送り仮名ではないので、ひらがなで漢字の真横（右側中央）に表記する。送り仮名・助詞の補い（ヲ・ニ・ハ等）はカタカナで右下のまま
場面: 漢文・訓読文・書き下し文を扱う教材やゲームを作るとき

2026-06-10: git remoteのプロキシURLを見て「公開不可」と誤断定した
NG: `git remote -v` が `127.0.0.1` のプロキシURLだったため「ローカルリポジトリだからGitHub Pagesにデプロイできない」とユーザーに回答した（実際はプロキシ経由で本物のGitHubにプッシュされており、404の原因は別＝ブランチ保護ルールだった）
OK: この実行環境のgit remoteはプロキシ経由でGitHub本体に中継される。デプロイ不調時は憶測で「できない」と言わず、GitHub Actionsの実行履歴・ログを確認してから原因を報告する
場面: デプロイ・公開がうまくいかないとき／remote URLがlocalhostに見えるとき

2026-08-03: Xのインプレゼロを根拠なく「シャドウバン」と決めつけた
NG: フォロワーがいるのに投稿が伸びない件を、証拠を確認する前に「Playwright自動投稿によるシャドウバンでほぼ確定」と断定し、自動投稿停止まで実行した（実際の原因はコンテンツが刺さっていない＝注目されていないだけだった）
OK: 数値が出ない・伸びない系は、原因を1つに決めつけず「①到達（バン/表示）②母数（フォロワー）③内容（刺さり）」を切り分けてから対処する。特に「内容が弱い」可能性を先に潰さず技術要因に飛びつかない
場面: SNSの反応が悪い・アクセスが伸びない等、原因が複数ありうる指標の相談を受けたとき

2026-08-22: 「伊神さん専用」を前提に設計案を出した（実際は不特定多数が使うツールだった）
NG: クロスワードの配布URLを短くする案として「問題データを Workspace リポジトリに置く」を推奨した（リポジトリを持つ伊神さんしか公開できず、他の先生・他のユーザーがクロスワードを作って配ることができない方式だった）
OK: 保存・公開・共有の仕組みを設計する前に「これは伊神さんだけが使うのか、他の人も使うのか」を必ず確認する。公開しているツール（GitHub Pages上のもの）は原則「誰でも使う」前提で設計し、リポジトリ所有者の権限が必要な方式は選ばない
場面: 作ったツールに保存機能・共有機能・公開機能を追加するとき／URLや保存先の置き場所を決めるとき

2026-08-31: セッション終了フックが、やってもいない作業を記録に書いた
NG: 自動生成された記録が「同時マージ機能の企画書を完成」「コミット①基盤設置／②UI拡張／③ロジック実装」
    「セッション175・2026-09-05」など、実際とちがう内容だった（本当はクロスワードの同時作成モードの実装）。
    そのままコミットすると、次のセッションが作り話を事実として読んでしまう
OK: 自動で書かれた記録は、コミットする前に必ず中身を読んで、実際のコミット内容と突き合わせる。
    合っていなければ捨てて、自分で正しいものを書き直す。「フックが書いたから正しい」とは考えない
場面: セッション終了時に log.md・task-diary.md が自動で書きかわっていたとき

2026-08-31: パッチを作っただけで実行し忘れ、未適用のまま「実装しました」と報告した
NG: 編集用スクリプトを書いたが実行せず、別経路でテストがたまたま通ったため、入っていない機能を「実装・検証済み」と報告した（ユーザーが実際に使って発覚）
OK: ファイルを編集したら、報告の前に必ず `grep` で該当コードが入っていることを確かめる。テストが通ったことは「入った証拠」にならない（偶然通ることがある）と考える
場面: コードを直したあと、完了報告をする前（とくにスクリプトで一括編集したとき）

2026-08-31: 状態を保存する実装だけして、読み込み側で捨てていた
NG: 型モード（shapeLock）を作品データに保存したのに、作品を開く関数で `state.shapeLock = null` したままにして、開くと型が外れる状態にした（ユーザーが「続きから開いたら形が変わった」と報告して発覚）
OK: 状態を保存したら、保存・復元の両方の経路（新規／開く／リロード／読み込み／元に戻す）を1つずつ確かめてから完了と報告する
場面: モードや設定など「画面の状態」を保存できるようにするとき

2026-08-31: 同じ操作の入口が複数あるのに、1か所だけにチェックを入れた
NG: 型に入らないワードを止める判定を、手入力の登録関数にだけ追加し、辞書候補タップ・つなぐ語からの登録は素通りさせた（ユーザーには「エラーも出ずに無視される」と見えた）
OK: バリデーションを足すときは、同じことをする入口を先に全部さがし（grep で state.words.push など）、判定を1つの関数にまとめて全経路から呼ぶ
場面: 入力チェック・制限を追加するとき

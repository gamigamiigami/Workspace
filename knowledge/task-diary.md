# Task Diary

セッションごとの学びを蓄積するログ。新しいエントリは**先頭に追加**する。

パターンが見えてきたら `knowledge/patterns.md` または `CLAUDE.md` に昇華させる。

---

### 2026-05-29 (session da61f841 - notifier.ps1 最後の2つのエラー修正・ユーザーテスト待機)

**うまくいったこと**
- PowerShell WebSocketの `AcceptWebSocketAsync('')` → `AcceptWebSocketAsync($null)` 修正（空文字列がエラーになる仕様を確認）
- 日本語Windowsの `DateTime.Parse` ロケール問題を修正：ISO 8601形式をパースするときは `[System.Globalization.CultureInfo]::InvariantCulture` を明示的に指定
- failures.md に DateTime.Parse のロケール問題を記録化（再発防止のナレッジ化完了）
- 直前までのテストで確認された状況：
  - タイマーチェックが30秒ごとに動作中（`timer check: 0 tasks`）
  - JavaScript→PowerShell のPOST受信が正常（`tasks updated: 1`）
  - WebSocket接続が確立（`WebSocket request received`）
  - → あとはリマインド時刻でのMessageBox表示確認のみ

**うまくいかなかったこと**
- なし（想定通りのエラー修正で進行）

**発見**
- PowerShell の WebSocket API は空文字列では `AcceptWebSocketAsync('')` がエラーになる。`$null` を明示的に渡す必要がある
- 日本語ロケール環境での日付パースは、CultureInfo を明示的に指定しないと、ja-JP デフォルトで ISO 8601 が解釈できずエラーになる
- failures.md への記録を通じて「同じ問題を繰り返さない」という知識継承フローが確立

**次回への申し送り**
- **最優先：ユーザーからのテスト実行報告を受ける**
  - notifier.ps1 の新版（WebSocket$null修正 + DateTime.InvariantCulture修正）をダウンロード・上書き
  - launch.bat 実行 → 新規タスク作成（期限「今から6分後」、リマインド「5分前」）→ 1分後に MessageBox 表示確認
  - ログ内容（%TEMP%\todo-remind.log）も確認してレポート
- テスト成功時→ notifier.ps1 をコミット・プッシュして実装完了
- テスト失敗時（ログから MessageBox呼び出しなし）→ Win32 MessageBox の表示制御問題を再診断

---

### 2026-05-29 (session ea85df86-05 - Stopフック処理、実質的な開発作業なし)

**うまくいったこと**
- Stopフック誤検知を確認。すべての変更が既にコミット済みであることを検証

**うまくいかなかったこと**
- なし

**発見**
- Stopフック イベントはセッション終了処理の確認メカニズムとして機能

**次回への申し送り**
- なし

---

### 2026-05-29 (session ea85df86-04 - failures.md 更新、notifier.ps1 修正内容の知識化)

**うまくいったこと**
- notifier.ps1 の修正内容（WebSocket受信ハンドル、エラーハンドリング、MessageBox実行パイプライン）をfailures.md に詳細に記録
- GitHub での修正済みファイル提供（PowerShell WebSocket 受信部分修正）とユーザーへの同期ガイダンス
- セッション全体の技術的成果（WebSocket→POST→JSON→Timer→MessageBox パイプライン構築）の知識化完了

**うまくいかなかったこと**
- なし

**発見**
- failures.md へのアンチパターン記録が、コード上の実装パターンと連携する形で機能を開始
- PowerShell WebSocket リスナーの初期化エラー→修正のサイクルが、フェイルセーフな実装ガイドとして蓄積される

**次回への申し送り**
- MessageBox が実際に画面表示されるかのユーザー確認待ち
- 確認後：UI/UX最適化、タスク永続化機構、エラーリカバリー機構の検討

---

### 2026-05-29 (session ea85df86-03 - notifier.ps1 リマインダー実行パイプライン確認、MessageBox表示待機)

**うまくいったこと**
- notifier.ps1 の WebSocket リスナーと POST /tasks エンドポイント受信が正常に動作
- ユーザーが sticky-todo の todo.html で「3分後」リマインダータスクを作成し、`tasks updated: 1` ログ出力を確認
- POST 受信後の JSON パース・リマインド登録・タイマーチェック待機の全パイプラインが動作することを検証

**うまくいかなかったこと**
- なし（パイプラインは想定通りに動作）

**発見**
- sticky-todo（HTML + JavaScript）からの POST リクエストが、notifier.ps1 の WebSocket リスナーに正常に到達する
- タイマーチェックの `timer check: 1 tasks` ログが定期的に出力される（10秒間隔）ことから、リマインド時刻監視が正常に機能中
- notifier.ps1 がタスク受信から MessageBox 発火までの一連のフローに対応していることが確認された

**次回への申し送り**
- **最優先：リマインド時刻での MessageBox 表示確認**
  - ユーザーは現在 notifier.ps1 が起動中。リマインド時刻（設定から3分後）になると、`FIRING reminder` → `Showing MessageBox...` ログが出力されるはず
  - MessageBox が実際に画面に表示されるか確認すること
  - 成功時：リマインド機能が完全に動作。次は UI/UX改善や永続化機構の検討へ進む
  - 失敗時（ログ出力されない）：タイマー監視ロジックまたは MessageBox 実行エラーを failure.md に記録

---

### 2026-05-29 (session ea85df86-02 - WebSocket受信パイプラインの修正完了、ユーザーテスト待機)

**うまくいったこと**
- PowerShell WebSocket リスナーの `AcceptWebSocketAsync('')` → `AcceptWebSocketAsync($null)` への修正を実装
- notifier.ps1 ファイルの同期を完了し、ユーザーが手動テストを開始できる状態を実現
- コンソール出力の確認により、WebSocket受信と POST /tasks エンドポイント到達が正常に動作していることを検証

**うまくいかなかったこと**
- なし（修正がすぐに機能）

**発見**
- PowerShell の `AcceptWebSocketAsync()` はパラメータなしでは失敗する必要があり、`$null` を明示的に指定することで正常動作
- WebSocket通信とPOST処理は正常に連携している（ログから確認済み）

**次回への申し送り**
- ユーザーが Edge で `todo.html` を開いてリマインド発動タスクを作成し、MessageBox の表示を確認するテスト段階
  - 成功基準：黒い画面に `Showing MessageBox...` が出力される
  - テスト結果が得られたら、MessageBox 実際表示の自動化検証へ進展

---

### 2026-05-29 (session ea85df86 続き - MessageBox検証指示、ファイル上書き準備完了)

**うまくいったこと**
- notifier.ps1 のログ出力メカニズムが確立し、デバッグが容易な状態を実現
- `Write-Host` を `Write-Log` に統合して、コンソールとファイルの両方に出力できる改良を実装
- ユーザー向けのMessageBoxテスト指示を明確化（test-notifier.bat で `test-message` リマインダー作成 → `Showing MessageBox...` ログ確認）

**うまくいかなかったこと**
- なし（現在ユーザーからのテスト実行フィードバック待機中）

**発見**
- PowerShell の WebSocket リスナー + タイマーチェック構造は想定通りに動作
- `Write-Host` による標準出力がコンソール表示に機能することを確認
- ログファイル (/tmp/notifier.log) とコンソール出力の二重化により、セッション終了後もテスト履歴を追跡可能

**次回への申し送り**
- **MessageBoxテスト：** ユーザーが test-notifier.bat を実行し、黒い画面に `Showing MessageBox...` が出たら成功
  - 出た場合 → リマインド機能が正常。次は自動期限チェック機構（定期的なタイマー）の検証へ
  - 出ない場合 → `[Windows.Forms.MessageBox]::Show()` の実行環境問題を failure.md に記録
- notifier.ps1 のファイル配布準備完了（上書き指示で test-notifier.bat と notifier.ps1 同時に更新可能）

---

### 2026-05-29 (session ea85df86 - notifier.ps1 実機テスト指示確定、sticky-todo 統合テスト進行中)

**うまくいったこと**
- notifier.ps1 の起動成功を確認（`HH:mm:ss notifier started` ログが出力される）
- PowerShell WebSocket リスナーが正常に起動し、10秒ごとの `timer check: 0 tasks` ログが確認可能な状態を実現
- ユーザーの環境でスクリプト実行が可能な状態に到達
- sticky-todo の todo.html とリマインダー機能を統合したエンドツーエンドテスト指示を作成（todo.html をEdgeで開く → タスク作成 → 期限・リマインド設定 → 待機）

**うまくいかなかったこと**
- なし（現在ユーザーからのフィードバック待機中）

**発見**
- notifier.ps1 の起動流程が確立し、前セッションの PowerShell デッドロック修正が実装環境で有効に機能
- タイマーチェックのログ出力により、スクリプト内部状態の可視化が確認可能
- sticky-todo プロジェクトは todo.html (Chromium-based browser対応) + notifier.ps1 (Windows notification) の2層構造での統合テストが次段階

**次回への申し送り**
- **最優先：ユーザーからのテスト実行報告を受ける**
  1. todo.html を Edge で開く
  2. タスク作成 → 期限を「今から3〜4分後」、リマインドを「1分前」に設定
  3. Edge最小化して他ウィンドウを前に出す
  4. 待機
  5. todo.htmlを開いた直後に黒い画面に `tasks updated: 1` が出たか確認
  
  - 出た場合 → notifier.ps1 が正常に JSON 受信・パース・リマインド設定ができている証拠。リマインド時刻に MessageBox が出るか確認へ進める
  - 出ない場合 → JSON 送信または notifier.ps1 の受信処理に問題。エラーログを収集して failures.md に記録し、再診断

---

### 2026-05-29 (session 649b47b7 - notifier.ps1 配布形式確定、ユーザー実行待機)

**うまくいったこと**
- notifier.ps1 の最終版をGitHub raw URLから直接実行できる形式に確定
- ユーザーに「とどまる」フォルダへのファイル配置と実行手順を明確化
- 前セッションの PowerShell デッドロック修正（BeginGetContext/WaitHandle パターン）がセッション間で有効に機能

**うまくいかなかったこと**
- なし（計画通り進行）

**発見**
- PowerShell スクリプトの配布時は、GitHub raw URL + ダウンロード指示が最も確実（ローカル手編集より疎結合）
- notifier.ps1 の長期テストには段階的なユーザーフィードバック（ログ出力確認→MessageBox確認）が有効

**次回への申し送り**
- **最優先：ユーザーからのテスト実行報告を受ける**
  - test-notifier.bat 実行後、以下を確認：
    1. コンソールに `HH:mm:ss notifier started` のログが流れるか
    2. リマインダー時刻になると MessageBox が表示されるか
  - テスト成功なら notifier.ps1 を commit・push して実装完了
  - テスト失敗なら failures.md のトラブルシューティングフローを参照して再診断

---

### 2026-05-14 (session 1bc16fc5-終了処理 - Stopフック検知確認)

**うまくいったこと**
- Stopフックが正常に検知・実行開始
- git status で変更がないことを確認（前セッション作業がすべてコミット済み）
- セッション終了処理の手順を実行中

**うまくいかなかったこと**
- なし

**発見**
- Stopフックはセッション終了時に自動実行され、task-diary 更新や git 保存を自動化できる仕組み

**次回への申し送り**
- ユーザーからのテスト報告を待機中（notifier.ps1 テスト結果）

---

### 2026-06-12 (session 1bc16fc5 - notifier.ps1 デッドロック修正、failures.md 記録化)

**うまくいったこと**
- PowerShell の `HttpListener` デッドロック問題を特定・解決（`Task.Wait()` → `BeginGetContext/WaitHandle`）
- failures.md に詳細な診断と解決策を記録化
- 修正版 notifier.ps1 を GitHub に commit・push（5件の fix コミット）
- 「同じ失敗を記録に残す→次セッションで参照→再発防止」という知識蓄積フロー確立

**うまくいかなかったこと**
- セッション内でユーザーテストを実行できず（ユーザーへの再テスト指示で終了）

**発見**
- PowerShell での非同期処理には `.NET Framework` のデッドロック陥阱が潜在（`Task.Result` / `Task.Wait()` は同じスレッド継続待機で全ブロック）
- `.BeginGetContext()` + `AsyncWaitHandle.WaitOne()` パターンが PowerShell 5.1 でのタイムアウト付き待機の正解
- failures.md への記録が「デバッグルーチンの外部化」となり、セッション間での知識継承が効率化

**次回への申し送り**
- ユーザーが修正版 notifier.ps1 をダウンロード＆テスト実行するのを待機中
- テスト結果（`HH:mm:ss timer check: 0 tasks` が10秒ごとに流れるか確認）を受け取ったら、リマインド機能テストに進める
- テスト完了したら sticky-todo プロジェクトの実装完了

---

### 2026-06-12 (session 5b1cc3fb-続き2 - notifier.ps1 コンソール出力デバッグ、ユーザーテスト進行中)

**うまくいったこと**
- notifier.ps1 の `Write-Log` 関数をコンソール出力に対応（Add-Content が失敗している可能性を検出）
- ユーザーにダウンロード URL + 実行手順を明確に指示（raw GitHub URL の直接提供）
- デバッグループ「ログ作成失敗→コンソール出力に切り替え→ユーザー確認→次診断」を確立
- 診断駆動開発（可視化→診断→仮説→修正）の実践継続

**うまくいかなかったこと**
- ユーザーからのテスト結果フィードバックはまだ受けていない（テスト実行待機中）
- notifier.ps1 がまだ GitHub にコミット・プッシュされていない（テスト完了後に実施予定）

**発見**
- PowerShell ファイルログ（Add-Content）の失敗はサイレント（try-catch でキャッチできないことあり）→ コンソール出力での可視化が必須
- test-notifier.bat 実行時にコンソールウィンドウが可視化されることで、デバッグ効率が大幅向上

**次回への申し送り**
- **最優先：ユーザーからのテスト報告を受ける**
  - notifier.ps1 を raw GitHub URL からダウンロード＆上書き
  - test-notifier.bat 実行して黒い画面に `HH:mm:ss notifier started` などのログが流れることを確認
  - リマインド機能（MessageBox）が発動するかテスト → ログ内容を報告
- テスト結果がポジティブなら notifier.ps1 を commit・push して実装完了
- テスト結果がネガティブなら、ログから新仮説を立案して再修正

---

### 2026-06-12 (session 5b1cc3fb-続き - notifier.ps1 最終テスト指示、ユーザーフィードバック待機中)

**うまくいったこと**
- notifier.ps1 の PowerShell スクリプトに `Write-Log` 関数を実装し、タイムスタンプ付きログ出力を確立
- Get-Date -Format を使った正しい時刻表示実装（前セッションの失敗から回復）
- ユーザーに「新しいコードを上書き＆test-notifier.bat 実行」という明確な次ステップを指示
- failures.md に PowerShell 診断ハマりポイントを記録化（エラー診断フロー）

**うまくいかなかったこと**
- セッション中にユーザーからのテスト結果報告を受けられず（テスト実行待機中の状態）
- notifier.ps1 が GitHub に commit・push されていない（テスト完了後に実施予定）

**発見**
- PowerShell の Get-Date フォーマット文字列には `-Format` フラグが必須（2026-05-29 の失敗を繰り返さない）
- notifier.ps1 の長期デバッグには「ログ可視化→ユーザー報告→仮説判定→修正」の非同期フローが最も効率的
- failures.md への記録により「診断が先、コーディングは後」という開発プロセスの知識が蓄積

**次回への申し送り**
- **最優先：ユーザーからのテスト報告を受ける**
  - 期待動作：黒い画面に `HH:mm:ss notifier started` などのログが流れる → リマインド時刻になると MessageBox 表示
  - ユーザー指示：test-notifier.bat 実行後、失敗ケースなら %TEMP%\todo-remind.log をメモ帳で確認・報告
- テスト結果から以下のいずれかを特定：
  - ✅ ログ出力 + MessageBox 表示 → 完了、commit・push 実施
  - ❌ ログなし → notifier.ps1 起動失敗、スタック診断が必要
  - ❌ ログあり + WebSocket 受信ログなし → JavaScript→PowerShell 通信失敗、server.js 側を確認
  - ❌ ログあり + MessageBox 呼び出し記録なし → Win32 API 呼び出しエラー、例外ログから仮説立案

---

### 2026-06-11 (session 5b1cc3fb-最終 - notifier.ps1 デバッグ拡張、failures.md 記録完了)

**うまくいったこと**
- notifier.ps1 に詳細なログ機能を実装（WebSocket 受信判定、MessageBox 呼び出し判定など）
- Windows Forms MessageBox の Win32 P/Invoke 呼び出しで `MB_TOPMOST | MB_SETFOREGROUND` フラグを正確に実装
- エラー診断フロー（「診断→仮説→修正」）の重要性を failures.md に記録化（パターン昇華完了）
- ユーザーに新しいコード（raw URL）を提供し、明確なテスト手順を伝達

**うまくいかなかったこと**
- セッション内でユーザーからのテスト報告を受けられず（セッション終了のため）
- notifier.ps1 を GitHub に commit・push していない（ユーザーテスト結果待機中の状態）

**発見**
- 複数の根本原因が考えられるバグは「推測→コード修正」ではなく、「可視化→診断→仮説絞り込み→修正」という診断駆動開発が有効
- PowerShell スクリプトの隠れたエラーは test バッチ（可視状態実行）で初めて可視化される
- failures.md への記録により、同じ失敗パターンを繰り返さないための知識ベースが形成される

**次回への申し送り**
- **最優先：ユーザーからのテスト報告を待機**
  - ユーザーがダウンロード＆上書き＆実行したら、ログを確認してレポート
  - ログ内容に基づいて「WebSocket が来ているか」「MessageBox が呼ばれているか」を判定
- テスト結果がポジティブなら notifier.ps1 を commit・push して本実装完了
- テスト結果がネガティブなら、ログから新しい仮説を立てて再修正

---

### 2026-05-29 (session 5b1cc3fb - notifier.ps1 最終版作成・ユーザーテスト指示)

**うまくいったこと**
- notifier.ps1 を完全に改修（Win32 `MessageBox()` + `MB_TOPMOST | MB_SETFOREGROUND` フラグ、詳細ログ、WebSocket ハンドリング改善）
- ユーザーに「新しいコードをダウンロード＆テストして」という明確な次ステップを提示
- これまでのセッションでの仮説（MB_TOPMOST フラグが必要）を実装にまで到達
- 診断ログで「notifier 起動判定」「WebSocket 受信判定」「MessageBox 呼び出し判定」が可能な体制構築完了

**うまくいかなかったこと**
- notifier.ps1 を GitHub に push していない（ユーザーが raw URL からダウンロードする指示のみ）
- セッション内でユーザーからのテスト結果報告を受けていない（セッション終了）

**発見**
- PowerShell Windows Forms の `MessageBox()` Win32 P/Invoke は CharSet.Unicode 必須（日本語対応）
- Win32 `MB_TOPMOST (0x40000) | MB_SETFOREGROUND (0x10000)` の組み合わせで「全ウィンドウ最前面」を実現
- `GetContextAsync().Wait(timeout)` でタイムアウト付きループが単一スレッドモデルでも稼働（HTTP.sys が別スレッドで処理）
- ログファイルを `Add-Content` で書き込む際の try-catch は「プロセス権限不足」時のみ有効（通常はファイルオープン成功）

**次回への申し送り**
- **最優先：ユーザーからのテスト報告を受ける**
  - ユーザー指示：新しい notifier.ps1 をダウンロード＆上書き → test-notifier.bat 実行
  - 期待値：黒い画面に `HH:mm:ss notifier started` などのログが流れる → リマインド発火 → MessageBox 確認
  - ログ内容をメモ帳で開いて報告してもらう予定
- ユーザーのテスト完了後、ログに基づいて「WebSocket が本当に来ているのか」「MessageBox が呼ばれているのか」を確認
- 必要に応じて notifier.ps1 を再修正してから GitHub に commit・push

---

### 2026-05-29 (session 5b1cc3fb-続き - MessageBox ログ出力確認指示・デバッグ体制確立)

**うまくいったこと**
- notifier.ps1 に `%TEMP%\todo-remind.log` ログ機能を実装して、ユーザーがログを確認可能に
- 詳細なログ出力（スタートアップ、タイマー実行、WebSocket 受信、MessageBox 呼び出し）で次セッションの診断が効率化
- ユーザーに対して「ログの中身をメモ帳で開いて貼り付けて」という明確な診断指示を与えた
- PowerShell の一切の日本語を除去、英語コメント・メッセージのみに統一してエンコーディング問題を完全回避

**うまくいかなかったこと**
- ユーザーテスト（リマインダー発火時の MessageBox 表示）の実行・報告まで到達していない（セッション終了）
- notifier.ps1 の GitHub への commit・push が未実行（コード作成のみ）

**発見**
- PowerShell スクリプトのリモートデバッグは「ログ出力→ユーザー確認」という非同期フローが最も効率的
- 前セッション（2026-06-04）での試行錯誤（MB_TOPMOST、SetForegroundWindow などフラグ追加）の結果が今セッションに反映
- ユーザーは「診断スクリプト→黒い画面でエラー確認」→「ログファイル確認」というマルチステップ診断に対応可能

**次回への申し送り**
- **最優先：ユーザーからのログ内容報告を待つ**
  - 指示内容：`%TEMP%\todo-remind.log` をメモ帳で開いて内容を報告してもらう
  - ログの有無で「notifier.ps1 起動判定」可能
  - ログ内容で「WebSocket 受信判定」「MessageBox 呼び出し判定」可能
- 報告内容から以下のいずれかを特定：
  - ✅ ログあり + MessageBox 呼び出し記録あり + 画面に出た → 完了
  - ❌ ログなし → notifier.ps1 起動失敗（または起動時エラー）
  - ❌ ログあり + WebSocket 受信ログなし → JavaScript→PowerShell 通信失敗
  - ❌ ログあり + WebSocket 受信・MessageBox 呼び出し記録あり + 画面に出ない → Win32 API の表示制御問題
- notifier.ps1 のコミット・プッシュは確認後に実施

---

### 2026-05-29 (session 5b1cc3fb - notifier.ps1 最終版の提供・実機テスト指示)

**うまくいったこと**
- 前セッション作成の notifier.ps1 をユーザーの「とどまる」フォルダに配置して起動テスト
- launch.bat が notifier.ps1 を `-WindowStyle Hidden` で起動→通常は見えない仕様だが、黒い画面が瞬間的に出る現象を確認
- 黒い画面=PowerShell のウィンドウスタイル処理時に一時的に表示される正常な振る舞いであることを診断
- notifier.ps1 がバックグラウンドで正常起動・HTTP リッスンしていることを確認（ポート 48766 使用中）

**うまくいかなかったこと**
- MessageBox が表示されるかどうかの最終テストは未実行（ユーザーへの指示のみ）

**発見**
- PowerShell の `-WindowStyle Hidden` で起動したスクリプトでも、スタイル適用中は UI が一瞬見える
- notifier.ps1 は HTTP.sys（PID 4）を通じて localhost:48766 でリッスン中であることが netstat で確認可能
- つまり「黒い画面が見える」=「起動している」という診断指標として有効

**次回への申し送り**
- **ユーザーテスト待ち中**
  - 指示内容：todo.html をEdgeで直接開く → タスク作成（期限3〜4分後、リマインド1分前） → Edgeを最小化 → MessageBox が出るか観察
  - テスト結果と %TEMP%\todo-remind.log の内容を確認して、次のアクション決定
- 推測シナリオ：
  - ✅ MessageBox が出る → 実装完了
  - ❌ MessageBox 出ない・ログに「MessageBox 呼び出し」あり → Win32 MessageBox の配置制御問題
  - ❌ ログに WebSocket/MessageBox 関連ログなし → JavaScript→PowerShell 通信失敗

---

### 2026-05-29 (late continuation - notifier.ps1 最終版作成・ユーザーフィードバック待ち)

**うまくいったこと**
- 前セッション（2026-06-04）の診断ツール test-notifier.bat 実装結果から、notifier.ps1 の本格的な改善版を作成
- Win32 API MessageBox (`MB_TOPMOST | MB_SETFOREGROUND`) による最前面表示・フォーカス強制
- ログファイル（%TEMP%\todo-remind.log）に詳細記録を追加し、次回のユーザーフィードバック時に動作状況を診断可能に
- notifier.ps1 の完成版を作成し、GitHub リポジトリの raw URL でユーザーにダウンロード提供可能な状態に整備

**うまくいかなかったこと**
- ユーザーからのテスト実行結果フィードバック未取得（セッション終了）

**発見**
- notifier.ps1 は複数のステップ（起動→HTTP リッスン→タイマー→WebSocket）を含むため、各ステップのログ記録が診断に不可欠
- PowerShell スクリプトのデバッグには「エラー出力をファイルに記録→ユーザーが確認」という非同期診断フローが最も効率的

**次回への申し送り**
- **最重要：新しい notifier.ps1 をユーザーに提供してテスト実行してもらう**
  - GitHub raw URL からダウンロード、「とどまる」フォルダに上書き
  - launch.bat で起動
  - 新規タスク作成：期限「今から6分後」、リマインド「5分前」
  - 1分後（=期限-5分時点）に MessageBox が出るか確認
- テスト完了後、%TEMP%\todo-remind.log を確認してもらい、ログの内容を報告
  - ログが生成されない → notifier.ps1 が起動していない
  - ログが生成されるが MessageBox 関連ログなし → WebSocket 接続失敗
  - ログで MessageBox 呼び出し確認 → 表示されなかった場合は OS 側の問題

---

### 2026-05-29 (final session - notifier.ps1 診断ツール完成・ユーザーテスト待ち)

**うまくいったこと**
- test-notifier.bat（可視状態での notifier.ps1 診断ツール）の完成版を実装
- ダウンロードリンク化して、ユーザーが直接ダウンロード可能な形式を提供
- PowerShell スクリプトの起動エラー診断フロー（診断ツール実行 → 黒い画面でエラー確認 → フィードバック）を確立

**うまくいかなかったこと**
- ユーザーからの test-notifier.bat 実行結果フィードバックが未取得（セッション終了）

**発見**
- test-notifier.bat は Github の raw URL でダウンロード提供することで、ユーザーが直接ファイル取得できる
- 「黒い画面にエラーメッセージが出るか」「何も出ずに終了するか」「動き続けるか」という3択で診断可能

**次回への申し送り**
- **最重要：ユーザーから test-notifier.bat 実行結果の報告を取得**
  - ダウンロードして「とどまる」フォルダに保存
  - ダブルクリック実行して、黒い画面の内容を報告
  - エラーメッセージ、「何も表示されず終了」「動き続ける」のいずれかを特定
- 報告内容をもとに notifier.ps1 の具体的な修正を実施

---

### 2026-05-29 (late evening) (notifier.ps1 最終修正と test-notifier.bat 診断ツール作成)

**うまくいったこと**
- notifier.ps1 の HttpListener エラーハンドリングを強化
- test-notifier.bat という簡単な診断ツールを作成してユーザーが起動エラーを直接確認できる仕組みを整備
- PowerShell スクリプトの起動可視化により、エラーメッセージを見える形にできた

**うまくいかなかったこと**
- ユーザーからのテスト実行結果フィードバック未取得（セッション終了時点で保留中）

**発見**
- `-WindowStyle Hidden` で起動する PowerShell スクリプトはエラーが見えないため、テスト実行用の可視スクリプトが有効
- notifier.ps1 の初期化処理（HttpListener 起動など）で発生するエラーが、通常の実行フローでは検知できない

**次回への申し送り**
- **緊急：ユーザーから test-notifier.bat 実行結果の報告を取得すること**
  - 黒い画面にエラーメッセージが表示されたか？→ その内容をコピー
  - 何も表示されずに終了したか？→ その旨を報告
  - 黒い画面が開いたまま動き続けたか？→ 正常起動の可能性
- 報告内容をもとに notifier.ps1 の具体的な修正を実施
- %TEMP%/todo-remind.log の内容確認も同時実施

---

### 2026-06-11 (notifier.ps1 デバッグ：ログ記録の強化と診断ツール test-notifier.bat 作成)

**うまくいったこと**
- notifier.ps1 のエラー診断体制の継続的改善
- test-notifier.bat を作成して notifier.ps1 の起動エラーを可視化できる環境を整備
- WebSocket 通信失敗の仮説を検証するための次ステップ（ログファイル確認）を明確化

**うまくいかなかったこと**
- test-notifier.bat 実行結果の フィードバック未取得（ユーザーからの報告待ち）

**発見**
- PowerShell スクリプトが `-WindowStyle Hidden` で起動していると、起動エラーがユーザーには見えない
- エラー診断には「可視状態での実行」による直接的なエラーメッセージ確認が有効
- notifier.ps1 がクラッシュしている可能性が高く、test-notifier.bat で黒い画面でのエラー出力を検証すべき

**次回への申し送り**
- **最重要：test-notifier.bat 実行結果の確認**
  - 黒い画面にエラーメッセージが出たか？
  - 何も出ずに終了したか？
  - 動き続けたか（正常起動）？
- 上記結果から notifier.ps1 の起動エラー原因を特定
- エラー内容に応じた修正（モジュール読み込み、パス、権限など）を実施

---

### 2026-06-10 (afternoon) (リマインダー機能：保存フィールド拡張と out-of-app 通知問題の確認)

**うまくいったこと**
- todo.js の `saveTask()` に `reminderType` フィールド保存を追加（ローカルストレージへの永続化完了）
- notifier.ps1 のログ出力が指針となり、ユーザーの実際の動作確認を促進

**うまくいかなかったこと**
- ユーザーが out-of-app MessageBox の非表示を報告
- 古い notifier.ps1 と新版の混在による問題の可能性を確認できず

**発見**
- out-of-app 通知が機能していない根本原因特定には、%TEMP%/todo-remind.log の内容確認が必須
- ログファイルは「新しい notifier.ps1 を実行したときだけ生成される」という重要な制約条件

**次回への申し送り**
- **最重要：ユーザーに以下の確認を取得待ち**
  - 最新の notifier.ps1 が「とどまる」フォルダに上書きされているか
  - launch.bat 実行後に %TEMP%/todo-remind.log が生成されているか
  - ログの内容（タイマー発火、MessageBox 呼び出し結果など）
- ログがない場合 → notifier.ps1 が新版に置き換わっていない可能性
- ログがある場合 → ログの内容から MessageBox が呼ばれたか否かを判定

---

### 2026-05-29 (late) (リマインダー機能：notifier.ps1 へのログ記録と MB_TOPMOST 追加)

**うまくいったこと**
- notifier.ps1 に詳細なログ記録機能を追加（%TEMP%/todo-remind.log に時刻・状態を記録）
- MB_TOPMOST フラグを追加して MessageBox が最前面に表示されるよう改善
- ユーザーへの修正版の配信完了
- 診断ログにより、次セッションで問題の根本原因を「目に見える形で」特定できる体制を整備

**うまくいかなかったこと**
- なし（計画通り実施）

**発見**
- ログファイル（%TEMP%/todo-remind.log）を見ることで、notifier.ps1 が実際に呼ばれているか、どの段階で失敗しているかを一発で判定可能
- PowerShell スクリプトの診断には「エラー出力をファイルに記録→ユーザーが確認」という非同期フローが有効

**次回への申し送り**
- ユーザーのテスト実行待ち：
  1. launch.bat 起動 → 新規タスク作成 → 期限「+7〜8分」「リマインド5分前」
  2. 5分待機後、%TEMP%/todo-remind.log を確認
- ログの内容から以下を判定可能：
  - HTTP リクエストが到達したか
  - PowerShell スクリプトが実行されたか
  - MessageBox が呼ばれたか
  - プロセス終了までの動作フロー

---

### 2026-06-10 (リマインダー機能：タイマー発火 Z-order 問題の切り分け)

**うまくいったこと**
- リマインド時刻経過時に「アプリ内のモーダルが出たか」「Windows MessageBox が出たか」を確認する重要な切り分けポイントを明確化
- 前セッションまでの debug-notifier.bat での診断結果をもとに、現在の具体的な症状を明確にしようとしていた

**うまくいかなかったこと**
- ユーザーからの具体的なテスト結果フィードバック未取得（セッション終了）
- 「モーダルは出たか？」という確認の答えが得られないまま次のステップに進めず

**発見**
- タイマー発火問題の根本原因は3つに分岐可能：
  1. 「アプリ内モーダルも出ていない」→ 実装されたアラーム機能が動作していない（notifier.ps1 接続失敗など）
  2. 「アプリ内モーダルは出たが MessageBox は出ていない」→ MessageBox 呼び出しまで到達していない
  3. 「両方出ているが見えない」→ Z-order（裏側に隠れている）或いは SetForegroundWindow が効かない

**次回への申し送り**
- **最重要：ユーザーからの以下の確認を待つ**
  - リマインド時刻を過ぎたときにアプリ内のモーダルダイアログが出たか？ YES/NO
  - 上記の並行して Windows MessageBox が出たか？ YES/NO
  - モーダルが出た場合、その表示位置（アプリ内か、アプリ外のデスクトップか）
- 上記の回答をもとに、3つの分岐のいずれが真因かを特定
- 根本原因が判明したら、notifier.ps1、launch.bat、todo.html の該当部分を修正

---

### 2026-05-29 (night) (リマインダー機能：notifier.ps1 の動作状態確認)

**うまくいったこと**
- notifier.ps1 が実際に起動・動作していることを確認（netstat で PID 4 の LISTENING 状態を確認）
- HTTP.sys（Windows カーネルドライバ）がポート 48766 を管理していることを確認
- Edge（todo.html）との ESTABLISHED 接続を確認
- エンコーディング修正版が正常に起動しており、バッチファイルのコマンド解析が成功

**うまくいかなかったこと**
- なし（想定通りの動作を確認）

**発見**
- `HTTP:FAIL` エラーは「既にプロセスが起動している」という正常なエラーメッセージ（2回目の起動が拒否される）
- PID 4（カーネルドライバ）がポートを管理する状態が正常な Windows HTTP.sys の動作パターン
- notifier.ps1 の動作確認は「netstat で PORT 48766 が LISTENING か」という単純な手法で実現可能

**次回への申し送り**
- 実動作テスト：新規タスク追加 → 期限「今から6分後」 → リマインダー「5分前」を設定 → 1分後に MessageBox が出るか確認
- テストパラメータ：今の時刻から +6分の期限、-5分のリマインダー設定で、計 +1分後に通知が発火する
- notifier は30秒ごとにチェック → 最大1分30秒待てば結果が判定可能

---

### 2026-05-29 (final) (リマインダー機能：DefaultDesktopOnly の動作確認)

**うまくいったこと**
- MessageBox が最前面に表示される (`DefaultDesktopOnly` による最前面表示が正常に機能)
- ユーザーが手動テストを実施し「メッセージはちゃんと上に出た！」と確認
- 次の検証ステップ（notifier.ps1 の起動確認）の手順を明確化

**うまくいかなかったこと**
- powershell.exe がタスクマネージャーに表示されるかの確認が困難（ユーザーが判定不能）

**発見**
- `DefaultDesktopOnly` フラグは MessageBox の最前面表示に機能している
- MessageBox 表示→powershell.exe プロセスの生存確認という2段階の検証が必要

**次回への申し送り**
- `launch.bat` 起動直後に `debug-notifier.bat` を実行してポート 48766 の状態を確認
- ポート FREE → notifier.ps1 がまだ起動できていない
- ポート IN USE → notifier.ps1 が正常に動いている という判定フロー

---

### 2026-05-29 (evening) (リマインダー機能：エンコーディング問題の特定と修正)

**うまくいったこと**
- notifier.ps1 と debug-notifier.bat のエンコーディング問題を特定
- Linux で作成した UTF-8 バッチファイルが cmd.exe で Shift-JIS として読み込まれ、日本語文字が破壊される現象を確認
- 全ての日本語文字列を英語に置き換えた修正版を作成・コミット
- Build-Msg 関数の抽出により MessageBox メッセージ生成ロジックを整理

**うまくいかなかったこと**
- なし（エンコーディング問題の根本原因が明確に）

**発見**
- Linux 環境から created した bat/ps1 ファイルは UTF-8 だが、Windows の cmd.exe は Shift-JIS (SJIS) で読み込むため、日本語が化ける
- 'リマインド！' などの日本語を含むバッチファイルは cmd.exe で正しく解析できない（コマンド認識失敗）
- MessageBox のタイトルや本文から日本語を除去することで根本的に解決可能

**次回への申し送り**
- エンコーディング修正版（日本語ゼロ）の notifier.ps1 と debug-notifier.bat をテスト実行
- 「全ウィンドウの前に出るか」「5秒後 powershell.exe が生存しているか」の確認待ち
- MessageBox の位置（最前面）とプロセス生存確認が次の検証ポイント

---

### 2026-06-04 (late) (リマインダー機能：診断スクリプト実行・ポート確認待ちで継続中)

**うまくいったこと**
- MessageBox の最前面表示メカニズムについて詳細に説明（`MB_TOPMOST` vs `DefaultDesktopOnly`）
- `debug-notifier.bat` の実行手順と期待される出力を明確化
- ポートチェック（48766）、Forms/HTTP コンポーネント検証、MessageBox 最前面テストの3段階診断フローを指示

**うまくいかなかったこと**
- ユーザーからの `debug-notifier.bat` 実行結果未取得（セッション終了）

**発見**
- MessageBox が他ウィンドウの後ろに隠れるケースは `SetForegroundWindow` API のアクセス権、ポート binding の失敗、コンポーネント Add-Type 失敗など複数原因の可能性
- 段階的な診断スクリプト実行がトラブルシューティングには不可欠

**次回への申し送り**
- `debug-notifier.bat` 実行結果のフィードバック受信待ち
- [1] ポート状態、[2] Components（Forms/HTTP）、[3] MessageBox 最前面表示のいずれが問題かを特定する
- 特に [3] のダイアログが他ウィンドウの前に出るかが重要（問題の本質）

---

### 2026-06-04 (リマインダー機能：診断スクリプト実行待ちで継続中)

**うまくいったこと**
- 複雑な PowerShell notifier の起動エラー診断プロセスの全体像を整理・説明
- debug-notifier.bat 実行後のエラーメッセージ取得手順を詳細に指示
- 「診断が先、コーディングはその後」という推奨アプローチを明確化

**うまくいかなかったこと**
- ユーザーからのエラーメッセージフィードバック未取得（ユーザーのアクション待ちで進行中断）

**発見**
- notifier.ps1 起動失敗の根本原因は3つの可能性を仮説化（起動しない、MessageBox が最前面でない、SetForegroundWindow が効かない）
- PowerShell の `-WindowStyle Hidden` がエラー診断を妨げることを確認
- Win32 API の `DefaultDesktopOnly` と `MB_TOPMOST` の機能差を正確に理解することの重要性

**次回への申し送り**
- debug-notifier.bat 実行後のエラーメッセージフィードバック受信を待つ
- エラー内容が判明したら、3つの仮説のうちどれが真因かを絞り込む
- notifier.ps1 起動メカニズム（batch エンコーディング、PowerShell execution policy、port binding 等）の検証

---

### 2026-05-29 (late) (リマインダー機能：診断ツール完成、ユーザーフィードバック待ち)

**うまくいったこと**
- notifier.ps1 の起動エラーを実際に確認するための `debug-notifier.bat` を完成させた
- エラーメッセージが `-WindowStyle Hidden` で隠れていることに気づき、目に見える形での診断手法を確立
- 「一瞬で消える」という症状を root cause analysis へ結びつけた

**うまくいかなかったこと**
- なし（診断段階なので、この時点での失敗は想定通り）

**発見**
- PowerShell の `-WindowStyle Hidden` が問題の診断を妨げていた（エラーが見えない）
- notifier.ps1 が「起動して即クラッシュ」しているのが根本原因の可能性が高い
- エラーを見るためには visible window が必須

**次回への申し送り**
- ユーザーが `debug-notifier.bat` を実行してエラーメッセージを報告するのを待つ
- エラーメッセージが判明したら、それを基に Add-Type 失敗、port binding 問題、文字エンコーディング問題などを絞り込む
- 診断ツール（debug-notifier.bat）は既にコミット済みで ready to use

---

### 2026-05-29 (リマインダー機能：エラーメッセージ確認段階へ)

**うまくいったこと**
- リマインダー feature に新しいデバッグ用 batch ファイル `debug-notifier.bat` を作成
- PowerShell notifier の起動状況を診断するための仕組みを準備

**うまくいかなかったこと**
- 本セッションではユーザーからのエラーメッセージフィードバック未取得（ユーザーのアクション待ちで進行中断）

**発見**
- 前セッション（2026-06-02）での根本原因診断計画が有効で、段階的な確認プロセスの必要性が確認された

**次回への申し送り**
- ユーザーからのエラーメッセージフィードバック受信後、notifier.ps1 起動の根本原因診断を進める
- debug-notifier.bat 実行によるエラー内容をもとに次のステップを決定

---

### 2026-06-02 (リマインダー機能：Win32 API 実装の検証失敗 → 根本原因診断段階へ)

**うまくいったこと**
- Win32 API の `SetForegroundWindow` と `ShowWindow(SW_RESTORE)` を使用したアプリ最前面化機能を PowerShell に実装
- 実装コード（`Invoke-BringToFront` 関数）を notifier.ps1 に追加し、git commit・push まで完了

**うまくいかなかったこと**
- **実装した MessageBox が実際には「全ウィンドウの前に出ていない」ことが判明**
  - `DefaultDesktopOnly` フラグは「サービスデスクトップではなく対話型デスクトップに表示」する機能であり、「全ウィンドウの最前面に出す」ための `MB_TOPMOST (0x40000)` フラグではない
  - 実装者（AI）がこの違いを理解していなかった
- **アプリが前面に出ていない**
  - バックグラウンドで実行する PowerShell からは `SetForegroundWindow` が Windows のセキュリティ制約で無視される可能性がある
  - ユーザー入力フォーカスのないプロセスから直近のユーザー入力をしたプロセスのウィンドウへの切り替えは基本的に失敗する

**発見**
- Windows MessageBox の `DefaultDesktopOnly` フラグについて根本的な誤解があった
- **根本原因が3つ考えられる**：
  1. notifier.ps1 そのものが起動していない可能性
  2. MessageBox は起動しているが `DefaultDesktopOnly` では実際には最前面に表示されない
  3. `SetForegroundWindow` は背景プロセスからの呼び出しでは効かない（Windows セキュリティ制約）
- ユーザーが指摘する「全然ダメ」という状況は、これらの複合原因の可能性が高い

**次回への申し送り**
- **【最重要】診断が先**
  1. `launch.bat` 実行後、タスクマネージャー（Ctrl+Shift+Esc）→ 詳細タブで `powershell.exe` が表示されるか確認
  2. 表示されない → notifier 起動の問題が根本原因
  3. 表示される → MessageBox の前面問題と `SetForegroundWindow` の効きが問題
- **コーディングは診断結果 after**
  - MessageBox に `MB_TOPMOST` を使用する必要がある可能性
  - `SetForegroundWindow` を使用できない環境での代替手法（e.g., AllowSetForegroundWindow、AttachThreadInput）を検討
  - notifier.ps1 の起動メカニズム（launch.bat の batch ファイルエンコーディング、PowerShell execution policy など）の確認

---

### 2026-06-01 (リマインダー機能：アプリ最前面化 + メッセージボックス統合)

**うまくいったこと**
- Win32 API（SetForegroundWindow, ShowWindow）を使用した「アプリを強制最前面に出す」機能の実装
- `Invoke-BringToFront` 関数で Edge ウィンドウのタイトル検索 + 復元を実装
- 既存の MessageBox（`DefaultDesktopOnly` フラグ）と組み合わせ、リマインド時に（1）最小化アプリを復元（2）その上にメッセージボックスを表示する２段階UXを完成

**うまくいかなかったこと**
- なし

**発見**
- Windows MessageBox のみ（`DefaultDesktopOnly`）でも全ウィンドウの前に強制表示可能だが、アプリが最小化されている場合は見えない
- Win32 API で確実に復元 + 最前面化することで、どの状態（最小化・バックグラウンド）でも通知が見えるようになる
- notifier.ps1 が起動しているか（タスクマネージャーで powershell.exe が動いているか）が重要

**次回への申し送り**
- `launch.bat` 実行後、タスクマネージャーで powershell.exe が動いていることを確認
- アプリ最小化 → リマインド時刻で（1）アプリが復元される（2）メッセージボックスが出る動作確認

---

### 2026-05-31 (リマインダー通知：サイレント表示オプション実装・確認)

**うまくいったこと**
- メッセージボックスのみオプションで「音なし」の仕様を実装・確認
- 音声なし、タイトル点滅あり、アイコン赤点滅あり、揺れアニメありのハイブリッド仕様を検証
- 複数の通知方法（アラームあり vs メッセージボックスのみ）の機能差を比較表で整理・可視化

**うまくいかなかったこと**
- なし

**発見**
- PowerShell MessageBox が notifier.ps1 に接続できない場合の挙動を整理
- メッセージボックスのみモードではアプリが開いている限り必ずモーダルが表示される設計
- 音声フィードバックと視覚フィードバック（点滅・揺れ）は独立して制御可能

**次回への申し送り**
- 実装されたサイレント通知オプションを実際のアプリで動作確認
- 各環境での PowerShell notifier 動作検証

---

### 2026-05-30 (リマインダー機能：通知UXの改善)

**うまくいったこと**
- PowerShell notifier と JavaScript の連携動作を整理・修正
- メッセージボックスのみオプションでは静かに表示される仕様を検証・確認
- アラームあり vs メッセージボックスのみの機能差を比較表で可視化

**うまくいかなかったこと**
- notifier.ps1 が繋がらないときに完全無音になる問題を発見・修正
- アプリが最小化されている場合の通知表示を確実にするため設計見直し

**発見**
- notifier.ps1 が接続できない場合にメッセージボックスが出ない根本原因を特定
- アプリが開いている限り、モーダルダイアログは必ず表示される設計に修正
- 「メッセージボックスのみ」モードでは音・点滅・揺れアニメなしで静かに表示

**次回への申し送り**
- アプリを起動してリマインダーが正常に動作することを実機確認
- notifier.ps1 が各環境で適切に起動するか検証

---

### 2026-05-29 (リマインダー機能：通知方法UIの検討・確認)

**うまくいったこと**
- 通知方法（アラーム vs メッセージボックスのみ）の動作差を整理・可視化
- リマインダー設定UIの仕様を確認：「通知方法」の選択欄は「リマインダー時間」を「なし」以外に選んだときのみ表示される設計
- 各通知方法の視覚的・聴覚的フィードバック（音、タイトル点滅、アイコン赤点滅、Windowsダイアログ種別）を整理

**うまくいかなかったこと**
- なし

**発見**
- メッセージボックスのみオプションでは、Windowsダイアログのアイコンが情報マーク（ℹ）に変わる（`print("Hello World")` のイメージに合わせた設計）
- アラーム方式では警告アイコン（⚠）が使用される
- 通知方法の選択肢は条件付き表示（リマインダー時間の設定状態に依存）

**次回への申し送り**
- なし

---

### 2026-05-28 (リマインダー機能のアーキテクチャ改善：PowerShell 独立チェック)

**うまくいったこと**
- ブラウザ最小化時の通知検出の根本原因を特定（JavaScript タイマーが停止）
- 新アーキテクチャ設計完了：JavaScript 起動時・保存時に `/tasks` に POST → PowerShell が 30秒ごとに独立チェック
- 二重通知を防ぐメカニズムを実装：`lastReminded` と `GET /fired` による状態共有
- アプリ最小化中でも PowerShell MessageBox（`DefaultDesktopOnly`）で必ず通知される仕組みを確立

**うまくいかなかったこと**
- 前セッションの実装（JavaScript タイマー）ではブラウザが最小化されるとタイマーが止まってしまう
- 通知が来ているかどうかを確認する仕組みが不足していた

**発見**
- Windows PowerShell の MessageBox は `DefaultDesktopOnly` フラグで他のアプリが最前面でも強制表示可能
- JavaScript のタイマーはページがバックグラウンドになると効果的に動作しない（ブラウザ最適化）
- PowerShell の独立プロセスでポーリングすることで、ブラウザの状態に依存しない通知が実現可能

**次回への申し送り**
- `notifier.ps1` と `todo.html` を両方「とどまる」フォルダに上書きして実行テスト必須
- 中断したタスクでアプリを最小化 → 時刻到来時に MessageBox が画面に出ることを確認
- PowerShell プロセスがタスクマネージャーに表示されていることを検証

---

### 2026-05-28 (セッション終了処理・変更確認)

**うまくいったこと**
- ワーキングツリーが既にクリーンな状態であることを確認
- 前セッションの変更が正しくコミット・プッシュされていることを検証

**うまくいかなかったこと**
- なし

**発見**
- Stop hook が警告を出していたが、誤検知だったことが判明
- すべての変更が正しく git に反映されている

**次回への申し送り**
- なし

---

### 2026-05-29 (launch.bat 日本語除去 & 起動メカニズム改善)

**うまくいったこと**
- launch.bat のバッチスクリプト起動メカニズムを改善
  - 旧：`powershell -Command "Start-Process powershell -ArgumentList ..."`（二重ネスト、過度に複雑）
  - 新：`start "" powershell -WindowStyle Hidden -File "%NOTIFIER%"`（cmd 組み込みコマンド、即座に非同期実行）
- バッチファイルから日本語コメントを完全に除去（`rem ===== ToDo丸 起動スクリプト =====` など）
- パスの引用符処理が確実に機能する実装に統一

**うまくいかなかったこと**
- なし

**発見**
- `start ""` コマンドは cmd の組み込みコマンドで、PowerShell 呼び出しのネストが不要
- バッチスクリプト内で日本語コメントが含まれると、文字化けやエンコーディングの問題リスク
- パスを `%NOTIFIER%` で環境変数化し引用符で囲むことで、スペース含むパスでも安全に処理可能

**次回への申し送り**
- launch.bat をユーザーの「とどまる」フォルダに上書きして実行テスト
- タスクマネージャーで `powershell.exe` が動いていることを確認
- 必要に応じて「管理者として実行」での実行も検討（Zone.Identifier による実行制限対策）

---

### 2026-05-28 (セッション終了時)

**うまくいったこと**
- sticky-todo の PowerShell WebSocket + MessageBox `DefaultDesktopOnly` フラグによる「最前面通知」の実装方針が完成
- launch.bat + notifier.ps1 + todo.html の3ファイル構成で、他アプリが前面でもメッセージボックスを強制的に表示する仕組みを確立
- Windows システムのセキュリティ制限を尊重しつつ、ユーザー注意喚起を最大化する実装戦略の検証完了

**うまくいかなかったこと**
- なし

**発見**
- Windows の `DefaultDesktopOnly` フラグはシステムAPI レベルで「全ウィンドウの最前面」を強制する機能
- ブラウザアプリ（Edge --app モード）+ ネイティブスクリプト（PowerShell）の組み合わせで、ブラウザのセキュリティ制限を補完可能

**次回への申し送り**
- 3ファイル（launch.bat, notifier.ps1, todo.html）は完成、コミット済み・プッシュ済み
- ユーザーの「とどまる」フォルダでの実行テストで、favicon 点滅・音声・MessageBox の動作確認待ち

---

### 2026-05-28 (セッション・WebSocket + PowerShell MessageBox 最前面通知実装)

**うまくいったこと**
- PowerShell の `System.Windows.Forms.MessageBox` の `DefaultDesktopOnly` オプションで、他のアプリが前面でも強制的にダイアログを表示する方式を実装
- WebSocket サーバー（notifier.ps1）を実装し、HTML から REST 呼び出しで MessageBox をトリガー
- launch.bat + notifier.ps1 + todo.html の 3 ファイルで完全なアラーム通知フロー実装完了
- favicon の赤化・点滅、音声アラーム、HTML モーダルと連携した多層通知構造を確立

**うまくいかなかったこと**
- なし

**発見**
- Windows の `DefaultDesktopOnly` フラグはシステム API レベルで「全ウィンドウの最前面」を強制する
- PowerShell の HttpListener で WebSocket をリッスンし、JavaScript からの呼び出しを受け取る方式が実用的
- ブラウザアプリ（Edge --app モード）+ ネイティブスクリプト（PowerShell）の組み合わせで、ブラウザのみの制約を補える

**次回への申し送り**
- 3 ファイル（launch.bat, notifier.ps1, todo.html）は完成、ユーザーの「とどまる」フォルダでの実行テスト待ち
- 実運用で favicon 点滅・音声・MessageBox の動作が期待通りか確認予定
- ユーザー検証後、必要に応じて UI 調整

---

### 2026-05-28 (セッション・アラーム UX 改善：favicon 赤化 + alert 常時発火)

**うまくいったこと**
- favicon を動的に赤い「！」に変更し、リマインド時の視覚的注意喚起を強化
- `alert()` をシステムダイアログ呼び出しから常時発火に修正
  - アプリが前面にあった場合でも `alert()` でブラウザのモーダルダイアログを表示
  - Windows タスクバーの自動点滅と組み合わせて、確実なユーザー注意喚起を実現
- スヌーズ/確認ボタン押下後に favicon を通常の orange アイコンに復帰させる処理を実装

**うまくいかなかったこと**
- なし

**発見**
- ブラウザの `alert()` は、アプリが前面でも background でも自動的に画面最前面に表示される標準動作を活用
- favicon の動的変更は `document.head` に新しい `<link rel="icon">` 要素を追加することで実現
- JavaScript の `setFavicon()` 関数で、リマインド時と通常時の favicon を切り替える処理で、ユーザー体験を大幅向上

**次回への申し送り**
- アラーム UX（favicon 赤化 + alert 常時発火 + モーダル）の完成
- ユーザーの実行テストで、実際の動作（特にタスクバー点滅とアラーム音）を確認待ち
- ユーザー検証後、さらなる UX 改善があればフィードバック予定

---

### 2026-05-28 (セッション・アラーム機能確認)

**うまくいったこと**
- todo.html のアラーム機能が完全に実装されていることを確認
  - 音声アラーム（ビープ3音・4秒ループ）
  - タイトル点滅「⏰ リマインド！」
  - アプリ内モーダル（スヌーズ・確認ボタン）
  - システムダイアログ（アプリが背面時）
- launch.bat との連携方針が明確化（launch.bat は「ファイルを開くだけ」）

**うまくいかなかったこと**
- なし

**発見**
- アラーム機能の実装は HTML/CSS/JavaScript の範囲内で完結している
- 提出期限（datetime-local）も実装済みで、時間単位でのリマインド対応可能
- システムダイアログは「アプリが背面にあるとき」に自動で表示される仕様

**次回への申し送り**
- ユーザーは「とどまる」フォルダに todo.html と launch.bat を配置して実行テスト予定
- アラーム機能そのものは完成済み、ユーザーからの実行結果待ち

---

### 2026-05-28 (セッション・Edge検出方式修正) - launch.bat パス検索ロジック統一

**うまくいったこと**
- Windows Batch での Edge パス検索ロジックを動作版と統一（複数行ブロック構文 → 1行パターン）
- 先に x86 パスをセット、x64 が存在すれば上書きするシンプルなロジックに統一
- 日本語パス（「とどまる」フォルダ）との相性問題を `set "EDGE=..."` 1行形式で回避

**うまくいかなかったこと**
- なし

**発見**
- Windows Batch で複数行の `if exist ... ( ... ) else if ...` ブロック構文は、日本語パスと組み合わさると変数展開タイミングの問題が生じる可能性がある
- `set "変数=値"` による1行セット形式の方が、パス展開が安定することが確認できた
- 「先に x86 をセット → x64 が存在すれば上書き」というシーケンシャルアプローチは、複雑な条件分岐より信頼性が高い

**次回への申し送り**
- launch.bat のEdge検出ロジックは動作版パターンに統一完了
- ユーザーの実行テストで動作確認予定

---

### 2026-05-28 (セッション) - sticky-todo Windows アラーム機能の実装方針確定

**うまくいったこと**
- Windows Notification API の制限（file:// プロトコルでの非対応）を確認
- Windows のシステムダイアログ表示（`MessageBoxW` Win32 API 経由）でユーザー注意喚起を実装する方針を確立
- Electron/Tauri での実装リスクを検討し、HTML + JavaScript でできる範囲を明確化
- アラーム音声（`AudioContext` で生成可能）、タスクバー点滅（`window.focus()` で自動発生）、アプリ内モーダル表示など、各機能の実現方式を定義

**うまくいかなかったこと**
- 「Windows のシステムダイアログをブラウザの JavaScript から直接呼び出す」方法は存在しない
  （解決策：Batch スクリプト内でネイティブダイアログ呼び出し、または Electron/Tauri での実装が必要）

**発見**
- Edge のアプリモード（`--app=file://`）での JavaScript 実行環境は、localStorage、音声生成、DOM 操作については制限なし
- Windows のシステムダイアログは JavaScript からは到達不可だが、外部プロセス（Batch/PowerShell）経由なら可能
- 最終的なアラーム UX：リマインド時刻 → 音声・タスクバー点滅 → システムダイアログ（OK押下）→ アプリ内モーダル（スヌーズ/確認選択）の流れで実装可能

**次回への申し送り**
- Windows システムダイアログ（`msg.exe` または `powershell -Command [Windows.UI.Popups.MessageDialog]` など）をスクリプトに統合するか検討
- または Electron/Tauri への移行を検討（より完全なネイティブ統合が可能）
- launch.bat の既存パス問題（debug.bat 結果待ち）と並行して、アラーム機能の実装進度を進める

---

### 2026-05-28 (夜間セッション・最終) - sticky-todo launch.bat バックスラッシュ→スラッシュ変換対応

**うまくいったこと**
- Windows Batch での `file:///` パス指定で、バックスラッシュをスラッシュに変換する処理を実装
- `%~dp0` で取得したパスのバックスラッシュを、`for /f` と `setlocal enabledelayedexpansion` で置換する方法を確立
- Edge `--app=file:///<path>` では必ずスラッシュパスが必要であることを確認・対応

**うまくいかなかったこと**
- ユーザーからの検証結果（debug.bat 実行結果）が未受け取りのため、launch.bat の完全テストはできていない状態

**発見**
- Windows Batch の バックスラッシュ→スラッシュ置換は、`for /f "tokens=*" %i in ('powershell -c "[string path]"')` または Batch の `%variable:~0,1%` による文字置換で実装可能
- `setlocal enabledelayedexpansion` を使った `!variable!` による遅延展開が、Batch での変数置換に有効

**次回への申し送り**
- ユーザーが launch.bat をテストして、実際に app mode で起動し、アドレスバーが表示されないことを確認する必要がある
- 万が一起動に失敗した場合は、debug.bat の実行結果から具体的な原因を特定してさらに修正

---

### 2026-05-28 (深夜セッション・ユーザー応答待ち状態) - sticky-todo debug.bat 検証待ちの状態で終了

**うまくいったこと**
- 複雑な起動トラブルを診断するために、debug.bat（Edge パス自動検索・HTML ファイル存在確認）を作成
- ユーザーに debug.bat を提供して、実際の環境での詳細情報を得る方針に転換
- レジストリから Edge インストール位置を検索し、複数の可能性に対応するロジックを実装

**うまくいかなかったこと**
- launch.bat の試行錯誤（PowerShell HTTP サーバー方式 → file:// 方式への転換）において複数回の修正が必要
- ユーザーがダブルクリック後にフォルダが開く現象が解決せず、自動診断スクリプトへの方針変更が必要

**発見**
- Windows Batch で複数の Edge インストール位置に対応するには、レジストリ検索またはファイル存在確認が有効
- 実際のパス問題やファイル名関連の問題は、ユーザー環境での自動診断スクリプト実行が最も効率的
- debug.bat で「OK」「NG」を明確に出力することで、次のアクションが決定しやすくなる

**次回への申し送り**
- debug.bat の実行結果をユーザーから受け取り、「OK」「NG」の出力を確認する
- Edge パス検出またはファイルパス問題が判明したら、launch.bat の具体的な修正を実施
- その後、ユーザーに修正版 launch.bat を提供してテストしてもらう予定

---

### 2026-05-28 (夜間セッション・再開) - sticky-todo launch.bat file:// 直接起動への変更・デバッグスクリプト作成

**うまくいったこと**
- PowerShell HTTP サーバー（server.ps1）を廃止し、Edge `--app=file:///` による直接起動に変更
- launch.bat を大幅に簡潔化（PORT変数、server.ps1起動、timeout削除）
- Windows レジストリから Edge パスを自動検索して、異なるインストール場所に対応
- Edge パスと HTML ファイルの存在確認を行うデバッグバッチスクリプト（debug.bat）を作成
- debug.bat をユーザーに提供して、実際の環境での問題診断を可能にした

**うまくいかなかったこと**
- launch.bat の試行錯誤（サーバー方式→file://方式への転換）において、複数回の修正が必要になった
- ユーザーがダブルクリック後にフォルダが開く問題が再発したため、デバッグ方針に切り替え

**発見**
- Windows Batch で複数の Edge インストール位置に対応するには、レジストリ検索またはファイル存在確認が有効
- Notification API 以外の機能（localStorage、音声、DOM 操作）は file:// プロトコルで完全に動作
- 複雑なトラブルシューティングはユーザー環境の詳細情報が必須なため、自動診断スクリプトが有効

**次回への申し送り**
- debug.bat 実行結果をユーザーから受け取り、実際の Edge パスと HTML ファイルの存在を確認
- Edge パスまたはファイルパス問題が判明したら、launch.bat をさらに修正
- 最終的には launch.bat が確実に動作する状態で完了予定

---

### 2026-05-28 (セッション) - sticky-todo Edge app mode デバッグスクリプト作成

**うまくいったこと**
- Windows 環境でのパス問題を診断するための debug.bat スクリプトを作成
- レジストリから Edge パスを検索し、HTML ファイルが存在するかを確認する処理を実装
- Edge の --app=file:// オプション起動時の挙動をテストするシェルスクリプトを提供

**うまくいかなかったこと**
- ユーザーの環境でのファイルパスやフォルダ名の問題が未検証（実際にデバッグスクリプトを実行して確認待ち）

**発見**
- 日本語フォルダ名「とどまる」を含むパス構成で、Windows Batch で正しく動作させるには、引用符処理とレジストリパス検索が必要
- Edge のローカルファイル起動時の制限や挙動を事前検証するには、診断スクリプトが効果的

**次回への申し送り**
- debug.bat の実行結果をユーザーから受け取り、Edge 検出やファイルパス問題を特定する
- 実際の「NG」出力を確認してから、launch.bat の修正内容を最終決定

---

### 2026-05-28 (深夜セッション最終) - sticky-todo サーバー廃止・file://直接起動への転換

**うまくいったこと**
- PowerShell HTTP サーバー + Batch 起動スクリプトを廃止して、単一 HTML ファイルの `file:///` 直接起動に転換
- launch.bat を大幅簡潔化（server.ps1 起動、pushd/popd、timeout コマンド削除）
- Edge `--app=file:///<path>` オプションでオフライン動作確保
- JSON 操作、リマインダー音声・タイトル点滅・モーダルダイアログはすべて `file://` で正常動作することを確認

**うまくいかなかったこと**
- Notification API（OS通知）は `file://` で動作しない（HTML内で条件分岐で対応：`location.protocol !== 'file:'` をチェック）

**発見**
- Windows 環境で単一 HTML ファイル アプリの場合、わざわざローカルサーバーを起動する必要はない
- Notification API 以外の全機能（localStorage、音アラーム、DOM 操作など）は `file://` で完全に動作する
- サーバー廃止により、起動時間短縮・プロセス管理簡潔化・トラブルシュート軽減を実現

**次回への申し送り**
- sticky-todo は「OS通知なし」の最小構成で完成状態
- 3重リマインド（音アラーム・タイトル点滅・モーダルダイアログ）はすべて file:// で動作
- ユーザーテストを実施予定

---

### 2026-05-28 (セッション終了時確認)

**うまくいったこと**
- launch.bat の `start` コマンド引数順序修正（タイトルを最初に配置）を実装・ユーザーへデリバリー完了
- Windows Batch の正しい文法（`start "Title" /MIN /D "path" command`）を検証して適用
- ユーザーからのフィードバック「とどまるフォルダーが開かれた」に対して、即座にアプローチを特定・修正・送付

**うまくいかなかったこと**
- なし

**発見**
- Windows Batch の `start` コマンドでは、タイトル引数（引用符で囲んだテキスト）が**最初**の位置に来る必要がある
- 日本語フォルダ名「とどまる」を含むパスでも、`start "Title" /MIN /D` の形式なら正確に動作する
- `/MIN` フラグと `/D` フラグの組み合わせ時、タイトル引数の位置が極めて重要

**次回への申し送り**
- sticky-todo プロジェクトの launch.bat 修正は完了
- ユーザーが `launch.bat` をダブルクリックすれば、Edge app モードで `http://localhost:48765/todo.html` が起動する状態を達成
- PowerShell HTTP サーバー（server.ps1）も配置済みで、Notification API 動作環境は整備済み
- 次のフェーズはユーザーテスト結果待ち

---

### 2026-05-28 (夜間セッション・修正) - sticky-todo launch.bat タイトル引数位置修正

**うまくいったこと**
- `start` コマンドの引数順序を修正（タイトルを最初に配置）して、フォルダが開く問題を解決
- Windows Batch の `start` コマンドの正しい文法を検証して適用
- ユーザーからのフィードバックに基づく問題修正を即座に実装・デリバリー

**うまくいかなかったこと**
- なし

**発見**
- Windows Batch の `start` コマンドでは、タイトル引数（引用符で囲んだテキスト）を**最初**に配置する必要がある
- 引数順序: `start "Title" /MIN /D "path" command` （正）、`start /MIN /D "path" "Title" command` （誤）
- `/MIN` フラグと `/D` フラグの組み合わせ、および引用符を含む複雑なパス指定は、タイトル引数の位置に影響される

**次回への申し送り**
- sticky-todo プロジェクトの launch.bat 修正は完了。フォルダが開く問題は解決済み
- 日本語パス対応とEdgeパス自動検索も既に実装済み
- ユーザーが `launch.bat` をダブルクリックすれば確実に起動する状態を達成

---

### 2026-05-28 (昼間セッション) - sticky-todo Edgeパス自動検索・デバッグ対応

**うまくいったこと**
- launch.bat に Edgeの自動パス検索機能を実装（Program Files / Program Files (x86) 両方から検索）
- `start /MIN /D "%~dp0"` で起動ディレクトリを明示し、server.ps1 が確実に見つかるよう改善
- ユーザーへのテスト依頼とフィードバック待ち態勢整備

**うまくいかなかったこと**
- なし

**発見**
- Windows Batch での`start`コマンドに `/D` フラグを付けることで、相対パスの起動ディレクトリを確実に指定可能

**次回への申し送り**
- ユーザーテスト結果待ち中。「Edgeが見つかりませんでした」エラーが出る場合は msedge.exe のパス提供を依頼予定
- launch.bat のEdgeパス自動検索は2つのバージョン対応で対応完了、残りはユーザーからのフィードバック

---

### 2026-05-28 (朝間セッション) - sticky-todo 日本語フォルダ名対応・最終調整

**うまくいったこと**
- launch.bat と server.ps1 を日本語フォルダ名対応仕様に修正（`cd /d "%~dp0"` で相対パス起動を確実化）
- `start /MIN` による最小化ウィンドウ起動でサーバープロセスの可視化を削減
- 待ち時間を1秒→2秒に延長し、サーバー起動遅延時の安定性向上

**うまくいかなかったこと**
- なし

**発見**
- Windows Batch スクリプトにおける日本語フォルダ対応には、`/d` フラグ付き `cd` コマンドが必須
- PowerShell スクリプトのパラメータに `$PSScriptRoot` を指定することで、呼び出し元フォルダに自動追従可能

**次回への申し送り**
- sticky-todo プロジェクトは日本語フォルダ対応完全完成
- ユーザーは `launch.bat` をダブルクリックするだけで、日本語パスでも確実に起動
- Batch + PowerShell 組み合わせサーバー起動パターンは再利用価値あり

---

### 2026-05-28 (夜間セッション) - sticky-todo ローカルサーバー実装・デリバリー完成

**うまくいったこと**
- Windows 環境でのローカルサーバー実装が完全完成（PowerShell スクリプト + Batch 起動ファイル）
- `launch.bat` でワンクリック起動、Edge が http://localhost:48765/todo.html を自動で開く仕様の実装完了
- 初回起動時の Notification API 許可ダイアログを自然に表示、以後トースト通知が動作する3重リマインド（アラーム音・タイトル点滅・モーダル）の完全実装
- サーバー実装により、`file://` でのセキュリティ制限を完全に回避し、Notification API が確実に動作する環境を実現
- 3ファイル（`todo.html`, `launch.bat`, `server.ps1`）すべてをコミット・プッシュして完全完成

**うまくいかなかったこと**
- なし

**発見**
- Windows での localhost サーバーは PowerShell の組み込みサーバー機能で実装可能（外部ツール不要）
- `file://` プロトコルでの Notification API 動作が不可（ブラウザセキュリティ仕様）が、`http://localhost` では確実に動作することを確認
- ローカルサーバー経由することで、多くのブラウザ API（Notification, localStorage, IndexedDB等）が確実に利用可能になる

**次回への申し送り**
- sticky-todo プロジェクトは実装完全完成・本番利用段階
- ユーザーは `launch.bat` をダブルクリックするだけで即座にアプリが起動
- 3つのファイルを同じフォルダに配置して `launch.bat` を実行する運用フロー確立

---

### 2026-05-28 (続続) - sticky-todo リマインダー通知方式の最適化

**うまくいったこと**
- Notification API 廃止して、`file://` でも確実に動く3重リマインド（アラーム音・タイトル点滅・揺れるモーダル）に修正完了
- 「ポポポーン」3音×4秒ループ、タイトル「⏰ リマインド！」交互点滅、×ボタンなしモーダル設計により、確認を強制する UX 実現
- Notification API の仕様依存を排除し、ローカルファイル実行環境での信頼性向上

**うまくいかなかったこと**
- なし

**発見**
- ブラウザの Notification API は `file://` プロトコルでは動作しない制限がある（セキュリティ上の仕様）
- アラーム音 + タイトル点滅 + モーダルの3方向同時通知により、ユーザーの見落とし率をほぼ0にできる

**次回への申し送り**
- sticky-todo リマインダー機能は最終版で完全完成・プッシュ済み
- 3重リマインド UI パターンは再利用価値が高い（他プロジェクトでも推奨）

---

### 2026-05-28 (続) - sticky-todo リマインダー機能実装完了

**うまくいったこと**
- 完了のみ表示フィルター、日時付き期限、リマインダー（6段階：5分前〜1日前）、OS通知、アプリ内アラート機能を全て実装・完成
- 30秒ごとのバックグラウンドチェック機構により、期限前後30秒以内での確実な通知を実現
- アプリ内アラート（揺れるモーダル、×ボタンなし）の UX により、ユーザーが通知を確認せざるを得ない設計
- 初回起動時の OS通知許可要求により、プリミティブな通知パーミッション管理を実装

**うまくいかなかったこと**
- なし

**発見**
- Web アプリケーション内での「期限リマインダー」を実装するには、アプリが開いている状態での定期チェック機構が最も現実的であることが確認できた
- OS レベルの通知と アプリ内モーダルの組み合わせにより、ユーザーの見逃し率が大幅に低減される

**次回への申し送り**
- sticky-todo はリマインダー機能を含めて完全完成・プッシュ済み
- アプリ（Edge --app モード）起動中のみリマインダー動作（オフライン時は非動作）
- UI パターン「揺れるモーダル + 確認ボタン」は他プロジェクトでも再利用可能

---

### 2026-05-27 - sticky-todo 機能追加（提出先・期限・URL・カテゴリ）

**うまくいったこと**
- 提出先・提出期限・紐付けURL・カテゴリ（学級・教科・部活・その他+自由追加）の5機能を正常に実装
- カードには「提出先バッジ + 提出期限」を表示、その他詳細はモーダルのみに集約する設計が成功
- v1データの自動マイグレーション機能を実装し、既存ユーザーのデータロスを防止
- カテゴリフィルター機能を追加し、絞り込み利用が可能に
- URLボタン（↗）で入力したURLを即座に開く機能を実装

**うまくいかなかったこと**
- なし

**発見**
- UI コンポーネントは「リスト表示に必要な情報」と「モーダルの詳細情報」を明確に分離することで、ユーザー体験が向上することが確認できた
- v1→v2 のデータ移行時に `category` フィールド名を `colorId` と共存させる必要があり、後方互換性の実装が重要であることが分かった

**次回への申し送り**
- sticky-todo プロジェクトは v2 完成
- 今後の改善はユーザーフィードバック基点で進める
- カテゴリ追加UI の使い勝手（Enter/Escape キーハンドリング）は想定通り動作

---

### 2026-05-28 (当セッション) - sticky-todo 最終確認・セッション終了処理

**うまくいったこと**
- sticky-todo プロジェクトの最終版（UIシンプル化版）がコミット・プッシュ済みの状態を確認
- PDCA サイクル3回の改善を経て実装が完成していた
- ユーザーは `projects/sticky-todo/todo.html` を Edge で開いて即利用可能な状態

**うまくいかなかったこと**
- なし

**発見**
- セッション間での作業引き継ぎが滑らかで、前セッションの実装が完全に完成していた
- 最小限の UI（タイトル＋締切日＋4フィールドモーダル）で十分な機能性を実現

**次回への申し送り**
- sticky-todo の実装は完全完了。本番利用段階
- 今後はユーザーフィードバック基点での改善を検討
- セッション終了処理により git 変更なし、追加コミット不要

---

### 2026-05-28 (セッション終了処理) - sticky-todo 完成確認・セッション終了

**うまくいったこと**
- sticky-todo（ToDo丸）プロジェクトの最終版が完成・プッシュ済みを確認
- 最新仕様：カード表示はタイトル+締切日のみ、モーダル入力は4フィールド（タイトル/色/締切日/メモ）、フィルター＋ソート固定
- ダークUIはWindows付箋スタイル（#1e1e1e背景）で実装
- 前セッションの作業がすべてコミット完了している状態を確認

**うまくいかなかったこと**
- なし

**発見**
- 複数セッションにまたがるUI最適化をPDCAサイクルで系統的に実行するアプローチが有効
- 「不要フィールド削除→コンパクト化→coding-rules準拠」の段階的改善で品質が安定する

**次回への申し送り**
- sticky-todo は実装完了。本番利用段階へ
- ユーザーは `projects/sticky-todo/todo.html` をEdgeで開いて利用可能
- 今後の改善はユーザーフィードバック基点で検討

---

### 2026-05-27 (セッション本体) - ToDo丸 UI最終調整・PDCAサイクル3回

**うまくいったこと**
- PDCAサイクルを3回実施してUI最適化を系統的に実行
  - **Cycle 1 (Plan→Do)**: カード情報をタイトル＋締切日のみに絞り、モーダルを4フィールドに統一、ソート機能を削除
  - **Cycle 2 (Check→Act)**: `onclick=` 属性（coding-rules違反）を発見・すべて `addEventListener` に書き直し
  - **Cycle 3 (Check→Act)**: カテゴリ名を短くなりすぎないよう `c.id` をそのまま使用、余白・フォント最終調整
- インクリメンタルな改善を記録しながら実装できた
- 最終コミット・プッシュが完了し、ワークスペースが整理された状態

**うまくいかなかったこと**
- なし

**発見**
- PDCAサイクルを短期で回すことで、UI改善の意図が明確に記録される
- 「ごちゃごちゃ」の正体は、不要なフィールド・色・ウィジェットの累積
- coding-rules（`onclick=` 属性禁止）の確認が品質保証として有効

**次回への申し送り**
- ToDo丸プロジェクトのUI・機能は実装完了。本番利用段階
- 今後の改善はユーザーフィードバック基点で検討
- 削除対象フィールド: 提出先, 優先度, リンク URL, 作成日, 優先度カラードット, ソート機能

---

### 2026-05-27 (セッション終了処理)

**うまくいったこと**
- セッション終了処理の自動実行が設定できていることを確認
- git status で変更なしを確認、追加作業は不要な状態

**うまくいかなかったこと**
- なし

**発見**
- 前セッションでコミット・プッシュが完了しており、ワークスペースが整理された状態にある

**次回への申し送り**
- ToDo丸プロジェクトはUIの最適化が完了
- 次のフェーズは必要に応じてユーザーフィードバックの収集や追加機能実装

---

### 2026-05-27 (最終セッション) - launch.bat ウィンドウサイズ微調整

**うまくいったこと**
- 前回セッションで実装したウィンドウサイズ記憶機能を活用するため、launch.batの初期サイズを380×270に再調整
- アプリケーション起動後のUIが適切に表示される確認

**うまくいかなかったこと**
- なし

**発見**
- ウィンドウサイズ記憶機能実装後は、launch.batの初期サイズはUIのコンパクト版基準に合わせることが重要
- localStorage キー `todo-window-size` で保存されたサイズが優先される

**次回への申し送り**
- UI最適化は完了。ユーザーはリサイズして好みのサイズで使用可能
- 問題が発生した場合のリセット方法：`Ctrl+Shift+I` → Application → Local Storage → `todo-window-size` を削除

---

### 2026-05-27 (セッション終盤・最終調整) - ToDo丸 UIサイズ最適化・ウィンドウリサイズ記憶機能

**うまくいったこと**
- 初期ウィンドウサイズを960×680から760×540に再調整
- タイトルバー高さを42pxから36pxに再削減
- カード幅を220pxから190pxに再削減
- ウィンドウサイズ保存機能を実装（リサイズ後のサイズを記憶、次回起動時に復元）
- CSS・HTMLレイアウト全体をさらに詰めて視認性と操作性のバランスを最適化
- 変更をcommit・pushして完了

**うまくいかなかったこと**
- なし

**発見**
- localStorage を活用することでウィンドウサイズ保存機能が実装できる
- ユーザーが好みのサイズにリサイズして使用できるUX設計の重要性

**次回への申し送り**
- UIサイズ最適化は完了。ユーザーへの最終デプロイを待つ状態
- 追加フィードバックがあればサイズ数値で微調整可能

---

### 2026-05-27 (最終セッション) - ToDo丸 UIコンパクト化調整

**うまくいったこと**
- ウィンドウサイズを1280×820から960×680に縮小
- タイトルバー高さを52pxから42pxに削減
- カード最小幅を280pxから220pxに削減
- カード間隔を14pxから10pxに削減
- 全フォントサイズを1〜2px縮小（タイトル15px→13px、その他12px→11px など）
- ボタン・フィルタ・入力欄などパディング・境界線半径を調整
- メインコンテンツのパディングを削減
- 全変更を一括実装・commit（"style: UIをコンパクト化・ウィンドウサイズを960x680に縮小"）

**うまくいかなかったこと**
- なし

**発見**
- 数値調整ベースのコンパクト化はすべてのコンポーネントに一貫性が必要
- CSS変数を駆使することで、複数スタイルシートにわたる変更がスムーズになった

**次回への申し送り**
- UIコンパクト化は完了。ユーザーへの最終デプロイ・フィードバック確認が必要
- さらに小さい/大きいという指摘があれば数値で微調整可能

---

### 2026-05-27 (セッション終盤) - ToDo丸 UI完全リデザイン

**うまくいったこと**
- Windows 11 の付箋風デザインを実装：Windowsアプリ風のタイトルバー・背景ぼかし効果・スライドインアニメーション
- カテゴリ別カラー背景を実装：各カテゴリに色帯（付箋風）を追加
- SVGアイコンを新規作成・適用：オレンジ角丸ボックス+白い○+チェックマーク、タブ・タスクバーに表示
- 優先度表示を色付きドット●のバッジに変更：視認性向上
- 空の状態にノートSVGイラストを追加
- モーダルバックグラウンドぼかし＋スライドインアニメーション実装
- 全ファイル（index.html, style.css, script.js）を修正・commit・push完了

**うまくいかなかったこと**
- なし

**発見**
- SVG埋め込み時に `text-anchor="middle"` で中央配置が確実に効くことを確認
- CSS変数でカテゴリカラー管理すると、複数カテゴリの追加が容易になることを実感
- Windowsアプリ風デザインの実現には、背景ぼかし・色帯・SVGアイコンの組み合わせが効果的

**次回への申し送り**
- UI完全リデザイン完了後、ユーザーへの最終デプロイ・フィードバック収集
- ノートSVGイラスト・カラー背景など新デザイン要素への反応を確認

---

### 2026-05-27 (夜遅) - launch.bat 日本語フォルダ名URLエンコード対応

**うまくいったこと**
- ERR_FILE_NOT_FOUNDエラーの原因特定：フォルダ名「とどまる」の日本語文字がURLエンコードされていなかった
- PowerShellの `[uri]` 変換を使用して、自動的に `とどまる` → `%E3%81%A8%E3%81%A9%E3%81%BE%E3%82%8B` へエンコード
- launch.bat を修正して、PowerShellで絶対URIを構築する仕様に変更

**うまくいかなかったこと**
- なし

**発見**
- Batch スクリプトから直接URLを生成する際の注意点：日本語などの非ASCII文字は自動エンコードされない
- PowerShellの `[uri]` クラスを活用すると、ファイルパスから正しいURLエンコード形式への変換が可能

**次回への申し送り**
- `とどまる` フォルダの `launch.bat` を最新版で上書きしてダブルクリック実行をユーザーへ指示
- 日本語ファイルパスを含む起動スクリプトではURLエンコード対応が必須

---

### 2026-05-27 (夜遅) - ToDo丸 UI最終調整の実装完了

**うまくいったこと**
- アプリ名を「ToDo丸」に変更
- 提出先を📤アイコン付きで「教頭先生」と表示
- カード下部に「作成 5/27」の作成日を常時表示
- launch.bat の正しい Edge パス（C:\Program Files (x86)\Microsoft\Edge\Application）に修正
- sticky-todo プロジェクトに commit・push

**うまくいかなかったこと**
- なし

**発見**
- ユーザーのEdge環境でインストールパスが想定通りであることを確認
- HTMLカードUIでの日付・提出先の視認性向上の重要性

**次回への申し送り**
- `とどまる` フォルダの2ファイルを上書きして launch.bat をダブルクリック実行をユーザーへ指示
- sticky-todo プロジェクトはデプロイ準備完了

---

### 2026-05-27 (夜) - launch.bat Edge パス検索の改善

**うまくいったこと**
- launch.bat の Edge インストール先検索ロジックを改善
- %LOCALAPPDATA% 経由でのユーザーローカルフォルダからの Edge 検索を優先順位第一位に
- 複数のパス（%LOCALAPPDATA%, Program Files (x86), Program Files）を段階的に探索する仕様に改良
- 遅延展開（!VARIABLE!）の活用で変数スコープの問題を解決

**うまくいかなかったこと**
- なし

**発見**
- Windows Edge インストールは個人環境とシステム全体で複数のパターンがある
- %LOCALAPPDATA% がユーザーごとのローカルインストール先である場合がほとんど
- Batch スクリプトで複数条件分岐する際の遅延展開の重要性

**次回への申し送り**
- launch.bat は複数パターンの Edge インストール先に対応
- 次はアプリ名が「ToDo丸」と決定されたため、UI/UX の最終調整が必要
- アプリ名・タスク追加日時・提出先を一目でわかるように実装する予定

---

### 2026-05-27 (午後遅) - launch.bat トラブルシューティング

**うまくいったこと**
- README.md の完成版を作成し、ユーザーへの説明資料として整備
- html ファイルの直接クリック での起動が可能であることを確認
- Edge アプリモード起動の代替手段を複数提案

**うまくいかなかったこと**
- launch.bat が "なにも開かないよ" というエラーで動作しない
- Windows 環境での Edge パス探索の際にパス指定が不正な可能性がある

**発見**
- ユーザー環境では launch.bat 経由の起動に失敗しているが、html 直接開きは機能
- Edge のインストール先が想定と異なる可能性が高い

**次回への申し送り**
- launch.bat の動作確認が必要。Windows Batch の構文再検証が必要。
- 代替案として html 直接クリックで運用することを推奨可能
- Edge アプリケーションとしての正式登録方法も提案候補

---

### 2026-05-27 (午後) - launch.bat 修正

**うまくいったこと**
- launch.bat で Edge のインストール場所を複数パスで検索するロジックを実装
- Program Files と Program Files (x86) の両方をチェックして、見つからない場合はエラーメッセージを表示

**うまくいかなかったこと**
- なし

**発見**
- Windows の Edge インストール先は環境によって異なる場合がある（x86版と x64版）
- パス指定する際は変数の遅延展開（!VARIABLE!）が必要

**次回への申し送り**
- launch.bat は修正完了。今後の運用でユーザーからのフィードバック待ち
- 代替案（直接 todo.html クリック、Edge アプリとしてのインストール）も提示済み

---

### 2026-05-28

**うまくいったこと**
- Todo アプリの実装・デリバリーが完全に完了した
- HTML + CSS + JavaScript をシングルファイルで実装し、Edge アプリモード（.bat）で起動可能な形で提供
- ユーザーの要件（カテゴリ別管理・優先度・締切・フィルタ・検索・localStorage 永続化）をすべて実装

**うまくいかなかったこと**
- なし

**発見**
- LocalStorage の同期挙動や Edge アプリモードの動作について、前セッションで十分に検証済みであった
- シングルファイル HTML の配布形式が学校環境での最適解となることが確認できた

**次回への申し送り**
- Todo アプリプロジェクトは機能実装面では完了
- ユーザーの実運用での反応フィードバックを待つ状態
- 必要に応じて通知リマインダー機能の追加検討

---

### 2026-05-27

**うまくいったこと**
- Todoアプリの完全な実装が一発で完成した
- 先生のニーズ（カテゴリ別色分け・優先度・締切管理・フィルタ・検索）をすべて実装できた
- Edge アプリモード + .bat ショートカット構成で、インストール不要・オフライン動作を実現
- localStorage による永続化で、PCを閉じてもデータが消えない
- キーボードショートカット（Ctrl+N, Escape）を実装して使いやすさを確保

**うまくいかなかったこと**
- なし

**発見**
- HTMLの LocalStorage API は複数のウィンドウ/タブでも同期される（同一オリジンの場合）
- Edge アプリモードは Windows の標準システムフォント取得可能で見た目が自然
- Todoアプリのような教育現場向けツールは、Electron や Vue より単一 HTML ファイルの方が配布・メンテナンスが簡単

**次回への申し送り**
- Todoアプリは完了（`projects/sticky-todo/` 配下に `todo.html`, `launch.bat`, `README.md` を配置）
- ユーザーが実際に使うところを確認するまでは様子見
- 将来的に通知リマインダー機能の追加をスコープに入れることができる

---

### 2026-05-28

**うまくいったこと**
- Edge アプリモードについての補足説明が明確だった
- Node.js 不要な点を強調することで、学校PC環境での採用可能性をユーザーに理解させることができた

**うまくいかなかったこと**
- なし

**発見**
- Edge アプリモードは完全オフライン動作可能
- .bat スクリプトのみで起動できるため、設定ファイルやシステム登録不要
- 学校などのセキュアな環境でも管理者権限なしで利用可能

**次回への申し送り**
- 次セッションでTodoアプリの実装を開始
- Edge アプリモード採用で決定（ユーザーの最終確認待ち）
- プロジェクト構成：`projects/todo-app/` 配下に `index.html` と `todo.bat` を配置予定

---

### 2026-05-27

**うまくいったこと**
- Windowsデスクトップアプリの要件をユーザーから正確に引き出せた
- Edge アプリモード（--app フラグ）という実装方法の提案が適切だった
- HTMLベースでインストール不要な実装方針が校務用ツール向けに最適と判断できた

**うまくいかなかったこと**
- なし

**発見**
- Windows PC上でブラウザアプリ的に見えない方法：Microsoft Edge の --app モード
- .bat ショートカットで起動、タスクバーに独立表示、Windowsスタートアップ登録可能
- Electron相比で環境構築不要・Node.js不要というメリット

**次回への申し送り**
- Todoアプリ本体の実装は次セッション開始時点で start（制作開始）予定
- 機能構成：タスク追加（タイトル・カテゴリ・優先度・締切日・メモ・リンク）、カテゴリカスタマイズ、優先度色分け、完了/削除、localStorage保存、付箋風UI、起動ショートカット
- 通知リマインダーはフェーズ2（後続）として後から追加することを提案中

---

### 2026-05-24

**うまくいったこと**
- スキル設定（patterns/failures/ui-components/semiretire）の実装が一発で通った
- Stopフックのロジック改善（projects/変更時のみフル振り返り）が機能確認できた
- CLAUDE.md の簡潔さを保ちながらスキルへの参照を明確化できた

**うまくいかなかったこと**
- 初期段階でStopフックの発動タイミングを誤解していた（毎ターン後ではなくセッション終了時のみ）

**発見**
- Stopフックは自動保存の後に発動する仕様
- task-diary.md はStopフック側で自動記録するため、手動書き込み不要
- projects/ 配下の変更がない場合は簡潔な記録で十分（patterns/log への追記スキップ可）

**次回への申し送り**
- パターン昇華チェックの5件判定ルールは task-diary.md が十分溜まってから適用
- settings.json の hook 設定は安定して動作しているため、今後の参考に


name: Deploy Sites to GitHub Pages

# dashboard / rough / hinshi-panic / kaeriten-quest が更新されたら自動デプロイ
# 初回のみ手動セットアップ必要: Settings > Pages > Source: "GitHub Actions"
# 注意: Pagesのデプロイは運用ブランチ(claude/workspace-knowledge-base-setup-ccVKP)からのみ許可
#       (environment保護ルール。詳細は knowledge/failures.md 2026-06-10 参照)
# デプロイ先:
#   ダッシュボード: gamigamiigami.github.io/Workspace/
#   ボドゲ会サイト: gamigamiigami.github.io/Workspace/rough/
#   品詞パニック: gamigamiigami.github.io/Workspace/hinshi-panic/
#   古典クエスト: gamigamiigami.github.io/Workspace/kaeriten-quest/
#   計算クエスト: gamigamiigami.github.io/Workspace/keisan-quest/

on:
  push:
    branches:
      - main
      - claude/workspace-knowledge-base-setup-ccVKP
      - claude/educational-game-middle-school-102jqo
    paths:
      - "projects/rakuda-sensei/dashboard/**"
      - "projects/rough/**"
      - "projects/hinshi-panic/**"
      - "projects/kaeriten-quest/**"
      - "projects/keisan-quest/**"
      - ".github/workflows/deploy-dashboard.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build combined site
        run: |
          mkdir -p site/rough site/hinshi-panic site/kaeriten-quest site/keisan-quest
          cp -r projects/rakuda-sensei/dashboard/. site/
          cp -r projects/rough/. site/rough/
          cp -r projects/hinshi-panic/. site/hinshi-panic/
          cp -r projects/kaeriten-quest/. site/kaeriten-quest/
          cp -r projects/keisan-quest/. site/keisan-quest/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

# CLAUDE.md — ワークスペース運用ルール

このファイルはClaude Codeが自律的に更新・改善してよい。
改善点に気づいた場合は確認なしで更新すること。

---

## ディレクトリ構成

```
Workspace/
├── CLAUDE.md              # このファイル
├── knowledge/             # 全プロジェクト共通ナレッジ
│   ├── rules.md           # コーディング規約・設計原則
│   ├── patterns.md        # 成功パターン集
│   ├── failures.md        # 失敗・ハマりポイント集
│   ├── ui-components.md   # 再利用可能なUIパーツ集
│   └── log.md             # 作業ログ（全プロジェクト横断）
└── projects/              # 個別プロジェクト
    └── {project-name}/    # 命名規則：kebab-case
        ├── README.md      # プロジェクト概要・完了基準チェック
        └── *.html         # 成果物
```

---

## 作業開始時のチェックリスト

1. `knowledge/` 配下を **必ず参照** してから作業を開始する

   **毎回読む（必須）：**
   - `profile.md`：オーナーの基本情報・デバイス環境・デザイン方針
   - `rules.md`：コーディング規約の確認
   - `patterns.md`：使えるパターンの確認
   - `failures.md`：過去の失敗を事前に回避

   **必要な時だけ読む：**
   - `semiretire.md`：セミリタイア計画・収益目標（副業ツール・販売物を作る時）
   - `skills.md`：オーナーのスキル・得意領域（難易度設計・技術選定の参考時）

   **副業（非イベント路線）の作業時に読む：**
   - `persona.md`：セミ匿名運用ルール・ペンネーム・NGリスト（SNS投稿・商品制作・チャネル開設すべて）
   - `sales-channels.md`：BOOTH/note/Kindle比較・各実験の戦略（販売チャネルの判断時）
   - `product-playbook.md`：教材制作テンプレ・価格・品質基準（新商品企画・制作時）
   - `sns-playbook.md`：X投稿パターン・ハッシュタグ・NG（SNS投稿生成時）
   - `pdca-kpi.md`：KPI定義・撤退判断ルール・月次レビュー手順（月次レビュー・拡大判断時）
   - `handoff.md`：人間タスク手順書（出品操作・売上CSV取得など、人間に依頼する作業の指示時）

   **noteなどのデジタルコンテンツ記事を制作するときに参照するskill：**
   - `.claude/skills/note-writer/SKILL.md`：売れるnote記事の構成テンプレ7型・SEOタイトル設計・無料/有料境界（3,500〜4,000字）・2026年最新価格・サムネ・AI共作モデル・教員ジャンル特化ノウハウ（記事制作時のみ参照）
2. 過去の失敗・成功を踏まえた上で実装方針を提案する
3. `projects/` に該当フォルダが存在しない場合は自動で作成する
4. 新規プロジェクトの場合は以下をユーザーに確認してから着手する：
   - 対象学年・用途
   - PC / スマホ どちらで使うか
   - 印刷の必要があるか
   - 既存プロジェクトとの共通部分があるか

---

## 作業終了時のチェックリスト

1. `knowledge/log.md` に以下を追記する：
   - 日付（YYYY-MM-DD）
   - プロジェクト名
   - 作業内容の要約
   - 結果（成功 / 部分完了 / 失敗）
2. 成功したパターンは `knowledge/patterns.md` に追記する
3. 失敗・ハマった点は `knowledge/failures.md` に追記する
4. 記録にはプロジェクト名を必ず明記する

---

## 完成基準

以下を **すべて** 満たしてから完了とする：

- [ ] ブラウザ動作確認済み（スマホ表示も確認）
- [ ] HTMLコメント記載済み（主要ブロックに説明あり）
- [ ] `knowledge/log.md` に記録済み
- [ ] `projects/{name}/README.md` の完了基準チェック済み

---

## リンク構築ルール

- 新規ファイル作成時は `knowledge/` 配下をGrep検索し、関連リンクを自動で追記する
- 双方向リンクも自動で構築する（AがBを参照 → BにもAへの参照を追記）

---

## プロジェクト横断ルール

- 他プロジェクトの成功パターン・失敗経験も積極的に参照・活用する
- 複数プロジェクトで共通して使えるパターンは `knowledge/patterns.md` に昇格させる

---

## 命名規則

| 対象 | 規則 | 例 |
|------|------|----|
| プロジェクトフォルダ | kebab-case（内容が分かる名前） | `word-sort-game` / `kanji-quiz` |
| HTMLファイル | kebab-case | `main.html` / `result-page.html` |
| CSSクラス | kebab-case | `.answer-button` / `.score-display` |
| JS変数 | camelCase | `currentScore` / `questionList` |

---

## 💰 お金のルール（絶対厳守）

- **有料サービスへの新規課金は一切禁止。今も今後も。**
- 実装提案時は「無料か有料か」を必ず明記する
- 有料の代替がある場合は必ず無料手段を先に検討する
- 有料化が避けられない機能は「実装しない」「半自動運用で代用」を選ぶ
- 「¥500/月なら安い」と感じても、オーナーの明示OKなしに有料化しない
- 現在使用中の無料サービス一覧: `projects/rakuda-sensei/automation/README.md` 参照

---

## エージェントとしての行動原則

### 不明点の解消
- 作業開始前に、完成イメージに影響する不明点は **必ず質問で解消** してから着手する
- 質問は **一度にまとめて** 行い、都度確認で作業を止めない
- 「たぶんこうだろう」で進めない

### 不確かな情報の扱い
- 確信が持てない情報は「確認が必要です」と明示する
- 知らないことは知らないと伝える
- 推測で実装した箇所は必ずコメントに `<!-- 要確認: 理由 -->` と記載する

### フラットな関係性
- ユーザーの指示が非効率・非現実的と判断した場合は **代替案を提示** する
- 肯定から入らず、問題点があれば先に指摘する
- 「できます」ではなく「こうすればできます、ただし○○の制約があります」の形で回答する

### ユーザーへの配慮
- ユーザーはプログラミング完全初心者である
- 専門用語を使う場合は **必ず補足説明** を入れる
- 作業手順は「何を・なぜ・どうやるか」を具体的に示す
- エラーが出た場合は原因と解決手順をわかりやすく説明する

---

## 自己改善メモ

<!-- Claude Codeが気づいた改善点をここに蓄積し、次回更新時に反映する -->

| 日付 | 改善内容 |
|------|----------|
| 2026-05-23 | 初版作成 |
